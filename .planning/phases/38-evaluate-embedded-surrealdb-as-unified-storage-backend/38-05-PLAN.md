---
phase: 38-evaluate-embedded-surrealdb-as-unified-storage-backend
plan: 05
type: execute
wave: 2
depends_on:
  - "38-01"
files_modified:
  - backend/pyproject.toml
  - backend/src/dotmd/storage/surreal_ops.py
  - backend/tests/storage/test_surreal_ops_safety.py
  - .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-SURREAL-PACKAGE-VERIFY.md
  - .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-EMBEDDED-SAFETY-GATE.md
autonomous: false
requirements:
  - STOR-04
must_haves:
  truths:
    - "D-01: Surreal schema/import/parity work cannot become migrate-ready until embedded surrealkv atomicity and single-writer safety are explicitly proven or marked blocking"
    - "The `surrealdb` package checkpoint is completed before any dependency install"
    - "Embedded commit, rollback, and writer-guard behavior are tested on copied/local Surreal stores with no live production mutation"
    - "Writer guard recovery is tested for stale owners through TTL and explicit force-release so a crashed spike process cannot permanently block the target path"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "prototype dependency on official `surrealdb` Python SDK after package verification"
      contains: "surrealdb"
    - path: "backend/src/dotmd/storage/surreal_ops.py"
      provides: "early embedded Surreal operations gate helpers"
      exports:
        - "SurrealEmbeddedSafetyReport"
        - "SurrealWriterGuard"
        - "probe_embedded_transaction_atomicity"
        - "probe_embedded_writer_safety"
        - "release_stale_surreal_writer_guard"
        - "force_release_surreal_writer_guard"
        - "write_embedded_safety_gate_report"
        - "assert_embedded_safety_gate_passed"
    - path: "backend/tests/storage/test_surreal_ops_safety.py"
      provides: "embedded atomicity and writer-safety gate tests"
    - path: ".planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-SURREAL-PACKAGE-VERIFY.md"
      provides: "package legitimacy checkpoint and source evidence"
    - path: ".planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-EMBEDDED-SAFETY-GATE.md"
      provides: "go/no-go evidence for embedded atomicity and writer safety"
  key_links:
    - from: "backend/src/dotmd/storage/surreal_ops.py"
      to: "backend/pyproject.toml"
      via: "uses verified `surrealdb` SDK only after package checkpoint"
      pattern: "from surrealdb import Surreal|surrealdb"
    - from: "backend/src/dotmd/storage/surreal_ops.py"
      to: ".planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-EMBEDDED-SAFETY-GATE.md"
      via: "writes explicit pass/fail gate evidence consumed by later plans"
      pattern: "write_embedded_safety_gate_report|embedded_safety_gate"
    - from: ".planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-EMBEDDED-SAFETY-GATE.md"
      to: ".planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-02-PLAN.md"
      via: "Plan 38-02 depends on this gate before schema/import work"
      pattern: "atomicity|writer|go_no_go"
---

<objective>
Create the early embedded SurrealDB go/no-go gate that must pass before storage schema, transform import, and retrieval parity work can claim migration feasibility.

Purpose: Research identified embedded transaction semantics and writer safety as the first technical gate. This plan resolves that before Plan 38-02 starts Surreal schema/import work, while preserving D-01's migration-first constraint and the `surrealdb` package checkpoint.
Output: Verified SDK dependency evidence, embedded atomicity/writer-safety helpers, tests, and `38-05-EMBEDDED-SAFETY-GATE.md`.
</objective>

<execution_context>
@/home/j2h4u/.codex/gsd-core/workflows/execute-plan.md
@/home/j2h4u/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-CONTEXT.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-RESEARCH.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-PATTERNS.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-VALIDATION.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-01-INVENTORY.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-01-MIGRATION-MAP.md
@backend/pyproject.toml
@backend/src/dotmd/ingestion/lock.py
@backend/src/dotmd/ingestion/trickle.py
@backend/src/dotmd/storage/base.py
@backend/tests/ingestion/test_pipeline_purge.py
</context>

<tasks>

<review_feedback_incorporation>
| Review finding | Incorporated here |
|---|---|
| Writer guard orphan cleanup needs TTL, force-release, or stale-owner recovery tests | Task 2 requires stale-owner TTL tests and explicit force-release behavior in `SurrealWriterGuard`; missing recovery evidence fails the embedded writer-safety gate. |
</review_feedback_incorporation>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Approve surrealdb package dependency</name>
  <files>backend/pyproject.toml</files>
  <read_first>
    - .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-RESEARCH.md
    - .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-CONTEXT.md
    - backend/pyproject.toml
  </read_first>
  <what-built>Research flagged package `surrealdb` as `SUS` because package legitimacy evidence is incomplete; no dependency should be installed before this checkpoint.</what-built>
  <how-to-verify>
    1. Inspect the PyPI package named `surrealdb`.
    2. Confirm its source repository is `github.com/surrealdb/surrealdb.py` or the current official successor linked by PyPI and SurrealDB's official Python docs.
    3. Confirm this package is acceptable as a Phase 38 prototype dependency despite the `SUS` audit disposition.
  </how-to-verify>
  <action>Block dependency installation until the human verifier explicitly approves the `surrealdb` package identity and source repository. If the verifier reports a mismatch, stop this plan before any `backend/pyproject.toml` edit.</action>
  <verify>
    <automated>! rg -n '"surrealdb|surrealdb' backend/pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - The checkpoint records explicit human approval before `backend/pyproject.toml` is edited.
    - The verifier checks PyPI package identity and source repository identity for `surrealdb`.
    - If package identity or source repository identity does not match the official SDK, the plan stops before dependency edits.
    - Before approval, `backend/pyproject.toml` does not contain a `surrealdb` dependency.
  </acceptance_criteria>
  <resume-signal>Type "approved surrealdb package" to continue, or describe the package mismatch.</resume-signal>
  <done>The package checkpoint is approved before install, or the plan stops before dependency edits.</done>
</task>

<task type="auto">
  <name>Task 1: Verify the SurrealDB Python SDK source and add the dependency</name>
  <files>backend/pyproject.toml, .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-SURREAL-PACKAGE-VERIFY.md</files>
  <read_first>
    - .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-RESEARCH.md
    - backend/pyproject.toml
    - .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-CONTEXT.md
  </read_first>
  <action>After the checkpoint approval, verify PyPI JSON metadata and the source repository URL for package `surrealdb`, confirm the source repository is `github.com/surrealdb/surrealdb.py` or the current official successor listed by PyPI, and write the evidence to `38-05-SURREAL-PACKAGE-VERIFY.md`. Then add the dependency with `cd backend &amp;&amp; uv add surrealdb`, preserving existing dependency style. If package metadata does not match the official SDK, stop the plan and write mismatch evidence instead of editing dependencies.</action>
  <verify>
    <automated>test -s .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-SURREAL-PACKAGE-VERIFY.md</automated>
    <automated>rg -n '"surrealdb|surrealdb' backend/pyproject.toml</automated>
    <automated>cd backend &amp;&amp; uv run python -c "import surrealdb; print(surrealdb.__name__)"</automated>
  </verify>
  <acceptance_criteria>
    - Package verification evidence includes package name, version, PyPI URL, source repository URL, and checkpoint approval note.
    - `backend/pyproject.toml` contains the new dependency only after verification evidence exists.
    - Existing dependencies remain in the current TOML dependency list style.
    - No unrelated packages are added.
  </acceptance_criteria>
  <done>The official SurrealDB Python SDK is verified, documented, installed in the repo environment, and importable through `uv run`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove embedded transaction atomicity and writer safety before schema/import</name>
  <files>backend/src/dotmd/storage/surreal_ops.py, backend/tests/storage/test_surreal_ops_safety.py, .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-EMBEDDED-SAFETY-GATE.md</files>
  <read_first>
    - backend/src/dotmd/ingestion/lock.py
    - backend/src/dotmd/ingestion/trickle.py
    - backend/tests/ingestion/test_pipeline_purge.py
    - .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-RESEARCH.md
    - .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-01-MIGRATION-MAP.md
  </read_first>
  <behavior>
    - Test `probe_embedded_transaction_atomicity` against a tmp_path `surrealkv://` store: committed multi-statement writes are visible after reconnect.
    - Test the same probe rolls back an injected failure with no partial records visible after reconnect.
    - Test `SurrealWriterGuard` and `probe_embedded_writer_safety` prevent a second writer guard for the same target path and expose owner metadata.
    - Test `SurrealWriterGuard` marks an abandoned owner stale after a caller-provided TTL and allows recovery only when the owner age exceeds that TTL.
    - Test explicit force-release requires matching target path and records the previous owner metadata in the gate report.
    - Test `write_embedded_safety_gate_report` records pass/fail status, evidence paths, and a downstream go/no-go value.
    - Test a WebSocket/server-backed result, if present, is labeled as a control result and cannot make the embedded gate pass by itself.
  </behavior>
  <action>Create failing tests first, then implement `SurrealEmbeddedSafetyReport`, `SurrealWriterGuard`, `probe_embedded_transaction_atomicity`, `probe_embedded_writer_safety`, `release_stale_surreal_writer_guard`, `force_release_surreal_writer_guard`, `write_embedded_safety_gate_report`, and `assert_embedded_safety_gate_passed` in `backend/src/dotmd/storage/surreal_ops.py`. Use tmp_path or copied Surreal store paths only. The probe must directly exercise embedded `surrealkv://` commit and rollback semantics through the verified SDK; do not use production `index.db`, `feedback.db`, FalkorDB, TEI, GLiNER, or live dotMD Docker volumes. Implement stale-owner recovery with a caller-provided TTL and explicit force-release for copied spike targets, and record previous owner metadata when recovery happens. If embedded atomicity, writer exclusion, or stale-owner recovery evidence fails, write `38-05-EMBEDDED-SAFETY-GATE.md` with a failing go/no-go value and stop before Plan 38-02 schema/import work.</action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/storage/test_surreal_ops_safety.py -x</automated>
    <automated>cd backend &amp;&amp; uv run python -m py_compile src/dotmd/storage/surreal_ops.py</automated>
    <automated>test -s .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-EMBEDDED-SAFETY-GATE.md</automated>
    <automated>rg -n "atomicity|rollback|writer|stale|TTL|force-release|go_no_go|surrealkv" .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-EMBEDDED-SAFETY-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Embedded commit and rollback behavior is tested on local/copied Surreal stores.
    - Writer guard behavior prevents concurrent same-target writers at the spike boundary.
    - Stale-owner TTL recovery and explicit force-release are tested and recorded in the gate report.
    - The gate report states whether downstream schema/import work may continue toward a migrate-ready result.
    - Failure of this gate blocks migration recommendation and is not hidden behind a WebSocket/server-backed control run.
    - No live production stores or production Docker volumes are mutated.
  </acceptance_criteria>
  <done>Plan 38 has an early embedded atomicity and writer-safety gate before Surreal schema/import/parity implementation.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

Files:
- `backend/pyproject.toml`
- `backend/src/dotmd/storage/surreal_ops.py`
- `backend/tests/storage/test_surreal_ops_safety.py`
- `.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-SURREAL-PACKAGE-VERIFY.md`
- `.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-EMBEDDED-SAFETY-GATE.md`

Classes and data structures:
- `SurrealEmbeddedSafetyReport`
- `SurrealWriterGuard`

Functions:
- `probe_embedded_transaction_atomicity`
- `probe_embedded_writer_safety`
- `write_embedded_safety_gate_report`
- `assert_embedded_safety_gate_passed`

CLI flags: none.
Config keys: none.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| package registry -> repo dependency | PyPI metadata and source links influence a repo dependency change. |
| local Surreal store -> gate report | Embedded database behavior becomes planning evidence for the rest of Phase 38. |
| writer process -> Surreal target path | Concurrent writers could corrupt or diverge embedded storage state. |
| WebSocket control run -> embedded decision | Server-backed behavior could be mistaken for embedded safety evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-38-05-SC | Tampering | `surrealdb` package install | mitigate | Blocking human checkpoint plus executor PyPI/source verification before modifying `backend/pyproject.toml`. |
| T-38-05-ATOM | Tampering / Availability | embedded transaction probe | mitigate | Test commit visibility and rollback absence of partial records after reconnect on `surrealkv://` tmp stores. |
| T-38-05-WRITER | Tampering / Availability | writer guard | mitigate | Use a target-specific local guard with owner metadata; tests reject a second same-target writer guard. |
| T-38-05-STALE | Availability | abandoned writer guard | mitigate | Provide caller-specified TTL stale-owner recovery plus explicit force-release for copied spike targets; record previous owner metadata and fail the gate if recovery is unproven. |
| T-38-05-SECRETS | Information Disclosure | package and gate reports | mitigate | Record package URLs, local tmp paths, and result status only; do not copy env values, tokens, or production paths containing secrets. |
| T-38-05-SCOPE | Integrity | WebSocket/server-backed control result | mitigate | Control results are labeled separately and cannot satisfy the embedded go/no-go gate. |
</threat_model>

<verification>
Overall checks:
- `test -s .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-SURREAL-PACKAGE-VERIFY.md`
- `cd backend && uv run python -c "import surrealdb; print(surrealdb.__name__)"`
- `cd backend && uv run pytest tests/storage/test_surreal_ops_safety.py -x`
- `cd backend && uv run python -m py_compile src/dotmd/storage/surreal_ops.py`
- `test -s .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-EMBEDDED-SAFETY-GATE.md`
</verification>

<success_criteria>
STOR-04's embedded atomicity and writer-safety prerequisite is resolved before Surreal schema/import/parity work starts: either the gate passes on embedded `surrealkv://` evidence, or downstream migration recommendation is blocked with explicit evidence.
</success_criteria>

<output>
Create `.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-SUMMARY.md` when done.
</output>
