---
phase: 38-evaluate-embedded-surrealdb-as-unified-storage-backend
plan: 02
type: execute
wave: 3
depends_on:
  - "38-05"
files_modified:
  - backend/src/dotmd/storage/surreal.py
  - backend/src/dotmd/ingestion/migrate_surreal.py
  - backend/tests/storage/test_surreal_storage_contract.py
  - backend/tests/ingestion/test_surreal_transform_only_migration.py
  - .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-02-IMPORT-PROOF.md
autonomous: true
requirements:
  - STOR-01
  - STOR-03
must_haves:
  truths:
    - "Plan 38-05 embedded atomicity and writer-safety gate passed before Surreal schema/import work proceeds toward a migrate-ready result"
    - "Surreal-backed storage records can represent current documents, source units, chunks, embeddings, entities, relations, feedback, cursors, and checkpoints"
    - "Transform import preserves existing chunk IDs, refs, fingerprints, source state, vector values, graph identities, and feedback rows without CPU-heavy recomputation per D-01"
  artifacts:
    - path: "backend/src/dotmd/storage/surreal.py"
      provides: "Surreal storage protocol implementation for the spike"
      exports:
        - "SurrealStoreConfig"
        - "SurrealConnection"
        - "SurrealMetadataStore"
        - "SurrealVectorStore"
        - "SurrealGraphStore"
        - "SurrealFeedbackStore"
        - "define_dotmd_surreal_schema"
    - path: "backend/src/dotmd/ingestion/migrate_surreal.py"
      provides: "transform-only SQLite/Falkor/feedback import proof"
      exports:
        - "SurrealImportMode"
        - "SurrealImportCounts"
        - "SurrealImportReport"
        - "load_sqlite_rows_for_surreal"
        - "load_graph_rows_for_surreal"
        - "load_feedback_rows_for_surreal"
        - "run_surreal_import"
    - path: "backend/tests/ingestion/test_surreal_transform_only_migration.py"
      provides: "migration invariant tests"
    - path: ".planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-02-IMPORT-PROOF.md"
      provides: "import coverage and recomputation boundary evidence"
  key_links:
    - from: "backend/src/dotmd/ingestion/migrate_surreal.py"
      to: "backend/src/dotmd/storage/surreal.py"
      via: "transform importer writes through Surreal store classes"
      pattern: "Surreal.*Store|run_surreal_import"
    - from: "backend/src/dotmd/storage/surreal.py"
      to: "backend/src/dotmd/storage/base.py"
      via: "classes implement existing storage Protocol method names"
      pattern: "add_chunks|search|delete_vectors_by_chunk_ids|get_related_sections"
    - from: "backend/src/dotmd/ingestion/migrate_surreal.py"
      to: ".planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-01-MIGRATION-MAP.md"
      via: "import coverage follows inventory map from Plan 38-01"
      pattern: "source_documents|resource_bindings|vec_meta|feedback"
    - from: "backend/src/dotmd/ingestion/migrate_surreal.py"
      to: ".planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-EMBEDDED-SAFETY-GATE.md"
      via: "schema/import proof requires early embedded go/no-go gate"
      pattern: "assert_embedded_safety_gate_passed|go_no_go"
---

<objective>
Build the Surreal-backed storage and transform-only import proof needed to evaluate STOR-01 and STOR-03 after the embedded atomicity and writer-safety gate passes.

Purpose: This plan tests whether the current dotMD data model can be represented in SurrealDB without falling back to chunking, embedding, or entity extraction work that D-01 forbids as the default.
Output: A Surreal storage module, a transform importer, and an import proof report.
</objective>

<execution_context>
@/home/j2h4u/.codex/gsd-core/workflows/execute-plan.md
@/home/j2h4u/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-CONTEXT.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-RESEARCH.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-PATTERNS.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-VALIDATION.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-01-MIGRATION-MAP.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-SURREAL-PACKAGE-VERIFY.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-EMBEDDED-SAFETY-GATE.md
@backend/pyproject.toml
@backend/src/dotmd/storage/base.py
@backend/src/dotmd/storage/metadata.py
@backend/src/dotmd/storage/sqlite_vec.py
@backend/src/dotmd/storage/vec_components.py
@backend/src/dotmd/storage/falkordb_graph.py
@backend/src/dotmd/ingestion/migration.py
@backend/src/dotmd/ingestion/migrate_fingerprints_to_blake3.py
@backend/tests/storage/test_metadata_m2m.py
@backend/tests/test_vector_delete.py
@backend/tests/storage/test_falkordb_graph.py
@backend/tests/ingestion/test_pipeline_purge.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED tests for Surreal schema and transform-only import</name>
  <files>backend/tests/storage/test_surreal_storage_contract.py, backend/tests/ingestion/test_surreal_transform_only_migration.py</files>
  <read_first>
    - backend/tests/storage/test_surreal_storage_contract.py
    - backend/tests/ingestion/test_pipeline_purge.py
    - backend/tests/storage/test_metadata_m2m.py
    - backend/tests/test_vector_delete.py
    - backend/tests/storage/test_falkordb_graph.py
    - backend/src/dotmd/storage/base.py
    - backend/src/dotmd/ingestion/migration.py
    - .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-01-MIGRATION-MAP.md
    - .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-EMBEDDED-SAFETY-GATE.md
  </read_first>
  <behavior>
    - Test `define_dotmd_surreal_schema` creates document, source-unit, chunk, embedding, entity, relation, feedback, cursor, and checkpoint record shapes.
    - Test `SurrealMetadataStore`, `SurrealVectorStore`, and `SurrealGraphStore` expose the existing Protocol method names used by dotMD callers.
    - Test `run_surreal_import(mode=DRY_RUN)` counts importable rows from deterministic SQLite/Falkor/feedback fixtures and writes nothing.
    - Test `run_surreal_import(mode=APPLY)` preserves chunk IDs, refs, text_hash values, row order where relevant, vector dimensions, entity names, relation types, feedback IDs, cursors, and checkpoints.
    - Test recomputation boundaries by monkeypatching embedding and entity-extraction call sites to raise if the importer reaches them.
  </behavior>
  <action>Create failing tests before implementing Surreal storage or import code. Build deterministic fixtures from current SQLite schema analogs and fake graph/feedback exporters. Per D-01, tests must prove the importer transforms existing stored rows and vector values as data. Tests must fail if the importer calls embedding, chunking, or entity-extraction services. Include dry-run, apply, row-count parity, unknown-category, rollback-on-error, and missing/failed Plan 38-05 embedded safety gate cases.</action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/storage/test_surreal_storage_contract.py tests/ingestion/test_surreal_transform_only_migration.py -x</automated>
  </verify>
  <acceptance_criteria>
    - RED tests fail on missing `dotmd.storage.surreal` and `dotmd.ingestion.migrate_surreal` symbols before implementation.
    - Tests cover every STOR-01 category named in REQUIREMENTS.md.
    - Tests contain explicit guards that fail on recomputation boundaries.
    - Tests use tmp_path and fake exporters; they do not require production Docker access.
  </acceptance_criteria>
  <done>RED Surreal schema/import tests exist and encode the transform-only storage contract plus the Plan 38-05 gate dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Surreal storage and transform import proof</name>
  <files>backend/src/dotmd/storage/surreal.py, backend/src/dotmd/ingestion/migrate_surreal.py, backend/tests/storage/test_surreal_storage_contract.py, backend/tests/ingestion/test_surreal_transform_only_migration.py, .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-02-IMPORT-PROOF.md</files>
  <read_first>
    - backend/src/dotmd/storage/surreal.py
    - backend/src/dotmd/ingestion/migrate_surreal.py
    - backend/src/dotmd/storage/base.py
    - backend/src/dotmd/storage/metadata.py
    - backend/src/dotmd/storage/sqlite_vec.py
    - backend/src/dotmd/storage/vec_components.py
    - backend/src/dotmd/storage/falkordb_graph.py
    - backend/src/dotmd/ingestion/migrate_fingerprints_to_blake3.py
    - backend/tests/ingestion/test_surreal_transform_only_migration.py
    - backend/src/dotmd/storage/surreal_ops.py
    - .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-EMBEDDED-SAFETY-GATE.md
  </read_first>
  <action>Implement `SurrealStoreConfig`, `SurrealConnection`, `SurrealMetadataStore`, `SurrealVectorStore`, `SurrealGraphStore`, `SurrealFeedbackStore`, and `define_dotmd_surreal_schema` in `backend/src/dotmd/storage/surreal.py`. Keep the module behind existing storage Protocol method names and do not wire it into `DotMDService` or `IndexingPipeline` as the default backend. Implement `SurrealImportMode`, `SurrealImportCounts`, `SurrealImportReport`, `load_sqlite_rows_for_surreal`, `load_graph_rows_for_surreal`, `load_feedback_rows_for_surreal`, and `run_surreal_import` in `backend/src/dotmd/ingestion/migrate_surreal.py`. Before applying schema/import writes, call `assert_embedded_safety_gate_passed` or equivalent Plan 38-05 gate validation using the gate report path provided by the caller. Use parameter binding for SurrealQL values, sanitize record IDs centrally, and preserve existing IDs in explicit fields when Surreal record IDs need escaping. Write `38-02-IMPORT-PROOF.md` with import counts from fixtures and any copied snapshot that is available. Per D-01, the importer must consume stored chunks, provenance, bindings, fingerprints, source state, vectors, graph rows, and feedback; it must not start a local indexing pipeline or derive replacement data from source markdown.</action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/storage/test_surreal_storage_contract.py tests/ingestion/test_surreal_transform_only_migration.py -x</automated>
    <automated>cd backend &amp;&amp; uv run pytest tests/storage/test_metadata_m2m.py tests/test_vector_delete.py tests/storage/test_falkordb_graph.py -x</automated>
    <automated>cd backend &amp;&amp; uv run python -m py_compile src/dotmd/storage/surreal.py src/dotmd/ingestion/migrate_surreal.py</automated>
    <automated>! rg -n "encode_batch|_embed_chunks|from_pretrained|index_single_file|index\\(" backend/src/dotmd/ingestion/migrate_surreal.py backend/src/dotmd/storage/surreal.py</automated>
    <automated>rg -n "38-05|embedded safety|go_no_go|assert_embedded_safety_gate_passed" .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-02-IMPORT-PROOF.md backend/src/dotmd/ingestion/migrate_surreal.py</automated>
  </verify>
  <acceptance_criteria>
    - All tests named in this task pass.
    - `run_surreal_import` returns counts for documents, source units, chunks, embeddings, vector components, entities, relations, feedback, cursors, and checkpoints.
    - Dry-run and apply modes share one counting path, and apply mode reports committed vs rolled-back status.
    - The grep gate finds no recomputation call sites in the Surreal import/storage modules.
    - Import apply mode refuses to produce migrate-ready evidence when the Plan 38-05 gate is missing or failed.
    - `38-02-IMPORT-PROOF.md` states which D-01 categories imported by transform and which remain unsupported or unsafe.
  </acceptance_criteria>
  <done>Surreal storage and import proof pass tests and document how much current data can move without CPU-heavy recomputation.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts This Phase Produces

Files:
- `backend/src/dotmd/storage/surreal.py`
- `backend/src/dotmd/ingestion/migrate_surreal.py`
- `backend/tests/storage/test_surreal_storage_contract.py`
- `backend/tests/ingestion/test_surreal_transform_only_migration.py`
- `.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-02-IMPORT-PROOF.md`

Classes and data structures:
- `SurrealStoreConfig`
- `SurrealConnection`
- `SurrealMetadataStore`
- `SurrealVectorStore`
- `SurrealGraphStore`
- `SurrealFeedbackStore`
- `SurrealImportMode`
- `SurrealImportCounts`
- `SurrealImportReport`

Functions:
- `define_dotmd_surreal_schema`
- `load_sqlite_rows_for_surreal`
- `load_graph_rows_for_surreal`
- `load_feedback_rows_for_surreal`
- `run_surreal_import`

CLI flags: none.
Config keys: none.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| copied snapshot -> Surreal embedded store | Existing production-derived data is transformed into a new database engine. |
| test fixtures -> migration evidence | Fixture evidence may be mistaken for production parity if not labeled. |
| query builder -> SurrealDB | Migration and storage code sends SurrealQL and record IDs to the database engine. |
| embedded safety gate -> import proof | Plan 38-05 go/no-go evidence controls whether import can be marked migrate-ready. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-38-02-SD | Tampering | import source snapshots | mitigate | Depend on Plan 38-01 snapshot manifest; import refuses incomplete or drifted inventory categories. |
| T-38-02-SQLI | Tampering | Surreal query construction | mitigate | Use parameter binding for values and a central record-ID sanitizer for identifiers; tests include malicious query text and record IDs. |
| T-38-02-SECRETS | Information Disclosure | package verification and import reports | mitigate | Record URLs and package metadata only; do not copy tokens, env values, or container secrets into reports. |
| T-38-02-PARTIAL | Tampering / Availability | transform import apply mode | mitigate | Import report records dry-run counts, committed counts, rollback status, category-level failures, and Plan 38-05 gate status; partial or ungated apply cannot be marked successful. |
| T-38-02-PARITY | Integrity | storage model completeness | mitigate | This plan proves representability and transform import only; Plan 38-03 decides retrieval parity. |
| T-38-02-SC | Tampering | `surrealdb` package install | mitigate | Depend on Plan 38-05 package checkpoint and verification before importing the SDK. |
</threat_model>

<verification>
Overall checks:
- `cd backend && uv run pytest tests/storage/test_surreal_storage_contract.py tests/ingestion/test_surreal_transform_only_migration.py -x`
- `cd backend && uv run pytest tests/storage/test_metadata_m2m.py tests/test_vector_delete.py tests/storage/test_falkordb_graph.py -x`
- `cd backend && uv run python -m py_compile src/dotmd/storage/surreal.py src/dotmd/ingestion/migrate_surreal.py`
- `test -s .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-02-IMPORT-PROOF.md`
</verification>

<success_criteria>
STOR-01 and STOR-03 are supported by a tested Surreal storage/import proof that preserves current data identities and reports transform coverage without using CPU-heavy recomputation as the default path.
</success_criteria>

<output>
Create `.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-02-SUMMARY.md` when done.
</output>
