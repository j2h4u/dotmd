---
phase: 41-production-grade-surreal-schema-and-import
plan: 03
type: tdd
wave: 3
depends_on: ["41-02"]
files_modified:
  - backend/src/dotmd/storage/surreal_ops.py
  - backend/devtools/surreal_migration_runner.py
  - backend/tests/storage/test_surreal_ops_safety.py
  - backend/tests/devtools/test_surreal_migration_runner.py
  - docs/surrealdb-production-migration.md
requirements: [SURR-MIG-02, SURR-MIG-03]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Maintainers can emit JSON and Markdown migration evidence reports that prove preserved counts, embedding reuse, no-recompute defaults, phase checkpoints, unsupported categories, and restore/rollback evidence."
    - "Restore evidence distinguishes official `surreal` CLI export/import availability from validated fallback copy evidence and never marks restore success without count/smoke verification."
    - "An operator/devtool runner exposes explicit plan, dry-run, apply, verify, and report modes plus overwrite-policy flags without adding runtime fallback or production cutover behavior."
    - "Documentation explains the Phase 41 operator flow and scope boundaries, including that old stores are read-only sources/evidence and that retrieval/cutover/deletion are not implemented here."
  artifacts:
    - path: "backend/src/dotmd/storage/surreal_ops.py"
      provides: "Restore manifest and migration evidence report helpers"
      exports: ["SurrealRestoreManifest", "SurrealMigrationEvidenceReport", "build_surreal_restore_manifest", "write_surreal_migration_evidence_reports", "classify_surreal_migration_report"]
    - path: "backend/devtools/surreal_migration_runner.py"
      provides: "Operator/devtool CLI for Phase 41 migration plan/dry-run/apply/verify/report execution against explicit inputs"
      exports: ["SurrealMigrationRunnerConfig", "run_migration_command", "build_parser", "main"]
    - path: "backend/tests/storage/test_surreal_ops_safety.py"
      provides: "Restore/report safety tests extended for Phase 41 manifest and false-success blocking behavior"
    - path: "backend/tests/devtools/test_surreal_migration_runner.py"
      provides: "Runner tests for CLI flags, report writing, no default apply, and malformed input handling"
    - path: "docs/surrealdb-production-migration.md"
      provides: "Phase 41 operator runbook and report schema documentation"
  key_links:
    - from: "backend/devtools/surreal_migration_runner.py"
      to: "backend/src/dotmd/ingestion/migrate_surreal.py"
      via: "builds config, calls run_surreal_migration, writes JSON/Markdown evidence"
      pattern: "from dotmd\\.ingestion\\.migrate_surreal import .*run_surreal_migration"
    - from: "backend/src/dotmd/storage/surreal_ops.py"
      to: "backend/src/dotmd/ingestion/migrate_surreal.py"
      via: "accepts SurrealMigrationReport-compatible data for evidence classification and restore manifests"
      pattern: "SurrealMigrationReport|migration_report"
---

<objective>
Create the Phase 41 migration evidence, restore, and operator reporting surface.

Purpose: The production migration runner needs durable evidence artifacts for data preservation and safety. This plan turns the runner output into operator-readable and machine-readable reports, restore manifests, and a repo-local devtool command without changing live runtime behavior.

Output: Ops/report helpers, a devtool runner with explicit flags, focused tests, and migration runbook documentation.
</objective>

## Artifacts this phase produces

This plan creates or updates these Phase 41 artifacts:

- Dataclasses: `SurrealRestoreManifest`, `SurrealMigrationEvidenceReport`, `SurrealMigrationRunnerConfig`
- Functions: `build_surreal_restore_manifest()`, `write_surreal_migration_evidence_reports()`, `classify_surreal_migration_report()`, `run_migration_command()`, `build_parser()`, `main()`
- Devtool CLI flags: `--mode plan|dry-run|apply|verify|report`, `--sqlite-snapshot`, `--target-url`, `--target-namespace`, `--target-database`, `--graph-export-json`, `--feedback-export-json`, `--gate-report`, `--overwrite-policy refuse|explicit-replace`, `--manifest-json`, `--report-json`, `--report-markdown`, `--restore-manifest-json`, `--owner-id`
- Report fields: `report_status`, `schema_version`, `mode`, `overwrite_policy`, `target`, `source_manifest`, `phase_checkpoints`, `expected_counts`, `actual_counts`, `embedding_reuse_verified`, `no_recompute_verified`, `unsupported_categories`, `restore_manifest`, `rollback_evidence`, `unresolved_blockers`, `recommendation`
- Documentation: `docs/surrealdb-production-migration.md`

No fresh package installs, production restarts, real retrieval implementation, shadow-run quality gate, production cutover, runtime fallback, or legacy deletion are part of this plan.

<execution_context>
@/home/j2h4u/.codex/gsd-core/workflows/execute-plan.md
@/home/j2h4u/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41-production-grade-surreal-schema-and-import/41-RESEARCH.md
@.planning/phases/41-production-grade-surreal-schema-and-import/41-PATTERNS.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-04-OPERATIONS.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-05-EMBEDDED-SAFETY-GATE.md
@.planning/phases/39-surrealdb-native-retrieval-contract/39-RETRIEVAL-CONTRACT.md
@.planning/phases/40-evaluation-harness-and-golden-queries/40-01-PLAN.md
@.planning/phases/41-production-grade-surreal-schema-and-import/41-02-SUMMARY.md
@backend/src/dotmd/ingestion/migrate_surreal.py
@backend/src/dotmd/storage/surreal_ops.py
@backend/src/dotmd/search/surreal_parity.py
@backend/tests/storage/test_surreal_ops_safety.py
@backend/tests/devtools/test_surreal_eval_runner.py
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED restore, report, and runner tests</name>
  <files>backend/tests/storage/test_surreal_ops_safety.py, backend/tests/devtools/test_surreal_migration_runner.py</files>
  <read_first>
    @backend/tests/storage/test_surreal_ops_safety.py
    @backend/tests/devtools/test_surreal_eval_runner.py
    @backend/src/dotmd/storage/surreal_ops.py
    @backend/src/dotmd/ingestion/migrate_surreal.py
  </read_first>
  <behavior>
    - `build_surreal_restore_manifest()` records source target path/url, backup path, restore path, method, CLI availability, expected counts, restored counts, smoke status, verified status, and notes.
    - `classify_surreal_migration_report()` blocks success when restore evidence is absent after apply, when phase checkpoints are failed/unverified, when embedding reuse is unverified, or when no-recompute evidence is false.
    - `write_surreal_migration_evidence_reports()` writes JSON and Markdown reports with schema version, mode, target identity, counts, phase checkpoint table, unsupported categories, embedding reuse, no-recompute evidence, restore manifest, rollback evidence, and unresolved blockers.
    - `backend/devtools/surreal_migration_runner.py` parses explicit flags for plan/dry-run/apply/verify/report modes and refuses apply without `--gate-report` plus explicit target inputs.
    - The runner never installs packages, never assumes the standalone `surreal` CLI exists, and reports CLI absence as evidence instead of masking it.
    - Malformed graph/feedback JSON export inputs fail with path and line/context in the error.
  </behavior>
  <action>Extend `backend/tests/storage/test_surreal_ops_safety.py` with Phase 41 restore/report classification tests. Add `backend/tests/devtools/test_surreal_migration_runner.py` modeled on `test_surreal_eval_runner.py`: use temp JSON fixtures, assert JSON/Markdown outputs, check explicit flags, and assert apply mode refuses unsafe invocations. The RED tests should fail because the Phase 41 ops dataclasses/functions and `devtools.surreal_migration_runner` do not exist yet. Keep all tests local and deterministic; use fake graph/feedback export JSON files and do not require live Docker, live production data, package installs, or the standalone `surreal` CLI.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/storage/test_surreal_ops_safety.py tests/devtools/test_surreal_migration_runner.py -x</automated>
  </verify>
  <acceptance_criteria>RED tests define restore manifest, evidence report, runner flag, malformed input, and unsafe apply behavior before implementation.</acceptance_criteria>
  <done>RED restore/report/runner tests are committed or recorded before implementation starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement evidence reports, restore manifests, and devtool runner</name>
  <files>backend/src/dotmd/storage/surreal_ops.py, backend/devtools/surreal_migration_runner.py</files>
  <read_first>
    @backend/src/dotmd/storage/surreal_ops.py
    @backend/src/dotmd/ingestion/migrate_surreal.py
    @backend/src/dotmd/search/surreal_parity.py
    @backend/tests/storage/test_surreal_ops_safety.py
    @backend/tests/devtools/test_surreal_migration_runner.py
  </read_first>
  <action>Implement the Phase 41 ops/report identifiers named in this plan. Extend `surreal_ops.py` without weakening the existing Phase 38 embedded safety and writer guard tests. `build_surreal_restore_manifest()` must capture official CLI availability if an explicit CLI path is provided, but it must also support validated fallback evidence when CLI is absent; false restore success is forbidden unless count and smoke verification pass. `classify_surreal_migration_report()` must return machine-readable blockers for failed phase checkpoints, missing restore evidence after apply, missing embedding reuse verification, false no-recompute evidence, unsafe overwrite, or partial failure. `write_surreal_migration_evidence_reports()` must write JSON with `ensure_ascii=False` and a Markdown summary suitable for Phase 43/44 evidence review. Implement `backend/devtools/surreal_migration_runner.py` with stdlib argparse and a direct-execution import bootstrap like existing devtools. It must parse the planned flags, build fake provider/exporter wrappers from JSON export files, call `run_surreal_migration()`, and write report artifacts through `surreal_ops.py`. Apply mode must require `--gate-report`, `--target-url`, and explicit source/export inputs; it must not call package installers, restart containers, run `dotmd index`, call TEI, run GLiNER, implement retrieval, perform shadow-run quality gates, switch runtime backends, or delete legacy code.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/storage/test_surreal_ops_safety.py tests/devtools/test_surreal_migration_runner.py tests/ingestion/test_surreal_production_migration.py -x</automated>
  </verify>
  <acceptance_criteria>Focused tests pass; JSON/Markdown evidence includes counts, phase checkpoints, embedding reuse/no-recompute fields, restore manifest status, and unresolved blockers; unsafe apply invocations fail closed.</acceptance_criteria>
  <done>GREEN report/runner implementation passes focused tests without dependency installs or live production changes.</done>
</task>

<task type="auto">
  <name>Task 3: Document the production migration operator flow and scope boundary</name>
  <files>docs/surrealdb-production-migration.md</files>
  <read_first>
    @AGENTS.md
    @.planning/ROADMAP.md
    @.planning/REQUIREMENTS.md
    @.planning/phases/41-production-grade-surreal-schema-and-import/41-RESEARCH.md
    @backend/devtools/surreal_migration_runner.py
  </read_first>
  <action>Create `docs/surrealdb-production-migration.md` as an operator/developer runbook for Phase 41. Document the explicit sequence: collect copied SQLite snapshot/exporter evidence, run plan, run dry-run, apply only with safety gate and explicit overwrite policy, run verify, emit JSON/Markdown reports, and retain restore/rollback evidence. Include the devtool flags and report schema fields from this plan. State that old SQLite/sqlite-vec/FTS5 + FalkorDB + feedback provider are read-only migration sources/evidence only; feedback rows must come from the supported provider/exporter path; stored embeddings are reused from sqlite-vec payloads; default TEI/reembedding/rechunking/GLiNER/indexing-pipeline recomputation is forbidden. State the scope boundary: Phase 41 does not implement real Surreal retrieval semantics, shadow-run quality gate, production cutover, runtime fallback, or legacy stack removal. Note that fresh installation of the standalone `surreal` CLI or a new package requires human verification because research flagged package legitimacy risk; the runner records CLI absence instead of assuming it is available.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/storage/test_surreal_ops_safety.py tests/devtools/test_surreal_migration_runner.py -x</automated>
    <automated>cd backend && just verify</automated>
  </verify>
  <acceptance_criteria>The runbook exists, matches the implemented devtool flags/report fields, preserves the user-specified phase scope, and the repo verify gate remains green.</acceptance_criteria>
  <done>Documentation and full verification are complete; no production restart or dependency install was performed.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | Phase 41 | Convert the Phase 38 schema/import proof into production migration tooling that preserves existing data where practical. | 41-01, 41-02, 41-03 | COVERED | This plan completes evidence/report/operator tooling around the schema and runner. |
| REQ | SURR-MIG-01 | Production schema represents all required categories. | 41-01, 41-02 | COVERED | Schema and import plans cover it; this plan reports the resulting category counts. |
| REQ | SURR-MIG-02 | Transform-first import avoids default recomputation. | 41-02, 41-03 | COVERED | This plan records embedding reuse and no-recompute evidence. |
| REQ | SURR-MIG-03 | Explicit backup, restore, rollback, and partial-failure semantics before cutover. | 41-02, 41-03 | COVERED | This plan creates restore manifests, report classification, and unsafe-apply runner gates. |
| RESEARCH | report-first migration | JSON and Markdown artifacts must be consumable by later quality/cutover evidence review. | 41-03 | COVERED | Tasks 1-2 create evidence report writers and tests. |
| RESEARCH | official restore direction | Use `surreal export/import` when available; validated fallback evidence only when CLI is absent. | 41-03 | COVERED | Restore manifest records CLI availability and verified fallback status. |
| RESEARCH | package legitimacy | `surrealdb` was flagged suspicious for fresh installs; standalone CLI is absent locally. | 41-03 | COVERED | No install tasks; docs and threat model require human verification before any install. |
| CONTEXT | user prompt | Data preservation verification and migration evidence/report artifacts are mandatory. | 41-02, 41-03 | COVERED | This plan writes report artifacts and verifies preservation/no-recompute evidence fields. |
| CONTEXT | user prompt | Production safety semantics must include dry-run, apply, verify, report, explicit overwrite policy, phase checkpoints, restore/rollback evidence. | 41-02, 41-03 | COVERED | Plan 41-02 owns runner semantics; this plan exposes and documents them. |
| CONTEXT | user prompt | `just verify` must stay green. | 41-03 | COVERED | Task 3 runs `cd backend && just verify`. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration reports -> operators / later phases | Reports may contain source refs, feedback messages, and graph metadata from production-derived data. |
| Devtool CLI arguments -> migration execution | Operator-provided paths and target URLs control source evidence, target writes, and report destinations. |
| Restore manifests -> safety decision | Restore evidence can incorrectly imply recoverability if counts or smoke checks are missing. |
| Long-running migration command -> local host resources | Production-derived imports can consume CPU, memory, and disk if not phase-bounded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-11 | Information Disclosure | JSON/Markdown migration reports | mitigate | Write reports only to explicit caller paths; document that reports can include source refs and feedback text; keep samples bounded and structured. |
| T-41-12 | Tampering / Data Loss | `surreal_migration_runner.py` apply mode | mitigate | Require explicit mode, target URL, source inputs, safety gate, and overwrite policy; refuse unsafe apply invocations. |
| T-41-13 | Repudiation | Restore manifest | mitigate | Require method, CLI availability, expected/restored counts, smoke status, verified status, and notes before classifying restore as verified. |
| T-41-14 | Denial of Service | Long migration commands | mitigate | Preserve named migration phases and checkpoint reporting; no service startup or per-request migration path is added. |
| T-41-15 | Tampering / Unsafe Rollback | Partial failure and rollback evidence | mitigate | `classify_surreal_migration_report()` blocks success when failed checkpoints, restore-required reports, unsafe overwrite, or missing restore evidence exist. |
| T-41-SC | Tampering | Package or `surreal` CLI installs | mitigate | No install is planned. If an executor needs to install the standalone `surreal` CLI or any new package, it must add a blocking human-verification checkpoint first because research flagged package legitimacy risk. |
</threat_model>

<verification>
Focused plan gate:

<automated>cd backend && uv run pytest tests/storage/test_surreal_ops_safety.py tests/devtools/test_surreal_migration_runner.py tests/ingestion/test_surreal_production_migration.py -x</automated>

Full phase gate:

<automated>cd backend && just verify</automated>
</verification>

<success_criteria>
- JSON and Markdown migration evidence reports include schema version, mode, overwrite policy, target identity, expected/actual counts, phase checkpoints, unsupported categories, embedding reuse, no-recompute evidence, restore manifest, rollback evidence, and unresolved blockers.
- Restore manifests cannot report success unless count and smoke verification pass.
- Devtool runner exposes explicit plan/dry-run/apply/verify/report modes and refuses unsafe apply invocations.
- The runner records standalone `surreal` CLI absence as evidence instead of attempting an install or assuming availability.
- Documentation matches implemented flags/report fields and repeats the scope boundary: no retrieval implementation, no shadow-run quality gate, no production cutover, no runtime fallback, no legacy deletion.
- Focused tests and `cd backend && just verify` pass.
</success_criteria>

<output>
Create `.planning/phases/41-production-grade-surreal-schema-and-import/41-03-SUMMARY.md` when done. Include RED/GREEN/REFACTOR notes, commits, report fields, runner flags, no-install evidence, and verification output.
</output>
