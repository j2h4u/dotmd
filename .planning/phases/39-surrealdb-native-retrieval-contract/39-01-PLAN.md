---
phase: 39-surrealdb-native-retrieval-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/dotmd/search/surreal_contract.py
  - backend/tests/search/test_surreal_contract.py
  - docs/surrealdb-native-retrieval-contract.md
  - .planning/phases/39-surrealdb-native-retrieval-contract/39-RETRIEVAL-CONTRACT.md
autonomous: true
requirements:
  - SURR-RET-01
  - SURR-RET-02
  - SURR-RET-03
  - SURR-MIG-02
must_haves:
  truths:
    - "The target retrieval contract is SurrealDB-native and does not define exact old-stack rank parity as the product goal"
    - "The old SQLite/sqlite-vec/FTS5 + FalkorDB stack is documented as baseline/evaluator only"
    - "Accepted difference categories are explicit: improvement, harmless reorder, regression, unclear"
    - "Regression handling is explicit and blocks later cutover unless fixed or deliberately accepted"
    - "The contract carries a migration constraint: preserve existing chunks, embeddings, source refs, graph relations, feedback, cursors, and checkpoints where practical"
    - "The contract forbids runtime fallback backends and productized compatibility shims after cutover"
  artifacts:
    - path: "backend/src/dotmd/search/surreal_contract.py"
      provides: "typed SurrealDB-native retrieval contract vocabulary for later evaluation/reporting phases"
      exports:
        - "AcceptedDifference"
        - "RetrievalSurface"
        - "CutoverGate"
        - "MigrationReusePolicy"
        - "SurrealRetrievalContract"
        - "default_surreal_retrieval_contract"
    - path: "backend/tests/search/test_surreal_contract.py"
      provides: "contract invariant tests"
    - path: "docs/surrealdb-native-retrieval-contract.md"
      provides: "human-readable SurrealDB-native retrieval contract"
    - path: ".planning/phases/39-surrealdb-native-retrieval-contract/39-RETRIEVAL-CONTRACT.md"
      provides: "phase-local contract evidence and handoff to Phases 40-43"
  key_links:
    - from: "docs/surrealdb-native-retrieval-contract.md"
      to: ".planning/REQUIREMENTS.md"
      via: "documents SURR-RET and migration/cutover constraints"
      pattern: "SURR-RET|SURR-MIG|SURR-CUT|SURR-DEL"
    - from: "backend/src/dotmd/search/surreal_contract.py"
      to: "backend/src/dotmd/search/surreal_parity.py"
      via: "replaces parity-only failure vocabulary with quality/difference vocabulary for future evaluators"
      pattern: "AcceptedDifference|RetrievalSurface|CutoverGate"
    - from: "docs/surrealdb-native-retrieval-contract.md"
      to: ".planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-03-RETRIEVAL-PARITY.md"
      via: "uses Phase 38 evidence as baseline lessons, not compatibility target"
      pattern: "weighted|hybrid|RRF|migration|baseline"
---

<objective>
Define dotMD's SurrealDB-native retrieval contract.

Purpose: Phase 39 prevents v1.8 from drifting back into exact compatibility work
or clean-room reindex assumptions. It creates a shared vocabulary for retrieval
surfaces, accepted differences, cutover gates, and migration reuse constraints.
Output: a small typed contract module, tests, and human-readable contract docs.
</objective>

<execution_context>
@/home/j2h4u/.codex/gsd-core/workflows/execute-plan.md
@/home/j2h4u/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.7-ROADMAP.md
@.planning/phases/39-surrealdb-native-retrieval-contract/39-CONTEXT.md
@.planning/phases/39-surrealdb-native-retrieval-contract/39-RESEARCH.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-RECOMMENDATION.md
@.planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-03-RETRIEVAL-PARITY.md
@backend/src/dotmd/search/surreal_parity.py
@backend/src/dotmd/search/fusion.py
@backend/src/dotmd/search/fts5.py
@backend/src/dotmd/search/semantic.py
@backend/src/dotmd/search/graph_direct.py
@backend/src/dotmd/storage/surreal.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED contract invariant tests</name>
  <files>backend/tests/search/test_surreal_contract.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/39-surrealdb-native-retrieval-contract/39-CONTEXT.md
    - .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-RECOMMENDATION.md
    - .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-03-RETRIEVAL-PARITY.md
  </read_first>
  <behavior>
    - Test the contract has retrieval surfaces for weighted full-text, vector, graph/entity, hybrid fusion, and reranker inputs.
    - Test accepted differences include improvement, harmless reorder, regression, and unclear.
    - Test regressions are blocking by default while improvements and harmless reorders are not.
    - Test unclear differences require human/explicit acceptance before cutover.
    - Test migration reuse policy defaults to preserving existing chunks, embeddings, source refs, graph relations, feedback, cursors, and checkpoints where practical.
    - Test the contract forbids runtime fallback backends and productized compatibility shims.
    - Test the old stack role is baseline/evaluator, not compatibility target.
  </behavior>
  <action>Create RED tests that import `dotmd.search.surreal_contract` and assert the contract invariants above. Keep tests pure and deterministic; no SurrealDB client or production data access is needed.</action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/search/test_surreal_contract.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail initially because the contract module does not exist.
    - Tests cover all three SURR-RET requirements and the migration carry-forward constraint.
    - Tests explicitly reject exact old-stack rank parity as a contract requirement.
  </acceptance_criteria>
  <done>RED tests define the SurrealDB-native retrieval contract invariants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the typed retrieval contract module</name>
  <files>backend/src/dotmd/search/surreal_contract.py, backend/tests/search/test_surreal_contract.py</files>
  <read_first>
    - backend/tests/search/test_surreal_contract.py
    - backend/src/dotmd/search/surreal_parity.py
  </read_first>
  <action>Implement a small dependency-free contract module using enums/dataclasses. Include retrieval surfaces, accepted difference categories, cutover gate behavior, migration reuse policy, and a `default_surreal_retrieval_contract()` factory. The module is not a SurrealDB adapter and must not import the SurrealDB SDK. It should be reusable by Phase 40 evaluation reports and Phase 43 shadow-run gates.</action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/search/test_surreal_contract.py -q</automated>
    <automated>cd backend &amp;&amp; uv run python -m py_compile src/dotmd/search/surreal_contract.py</automated>
    <automated>rg -n "surrealdb|Surreal" backend/src/dotmd/search/surreal_contract.py backend/tests/search/test_surreal_contract.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests pass.
    - The module has no runtime dependency on SurrealDB, SQLite, FalkorDB, or TEI.
    - The default contract marks old-stack output as baseline/evaluator only.
    - The default contract records no fallback backend and no compatibility shim policy.
    - Migration reuse policy lists stored data categories that should be preserved where practical.
  </acceptance_criteria>
  <done>Typed contract module exists and passes invariant tests.</done>
</task>

<task type="auto">
  <name>Task 3: Write human-readable contract and phase handoff</name>
  <files>docs/surrealdb-native-retrieval-contract.md, .planning/phases/39-surrealdb-native-retrieval-contract/39-RETRIEVAL-CONTRACT.md</files>
  <read_first>
    - backend/src/dotmd/search/surreal_contract.py
    - .planning/REQUIREMENTS.md
    - .planning/phases/39-surrealdb-native-retrieval-contract/39-CONTEXT.md
    - .planning/phases/39-surrealdb-native-retrieval-contract/39-RESEARCH.md
    - .planning/phases/38-evaluate-embedded-surrealdb-as-unified-storage-backend/38-03-RETRIEVAL-PARITY.md
  </read_first>
  <action>Write `docs/surrealdb-native-retrieval-contract.md` as the durable architecture contract and `39-RETRIEVAL-CONTRACT.md` as the phase-local evidence/handoff. Keep the doc concise. It must specify retrieval surfaces, old-stack baseline role, accepted-difference categories, cutover-blocking rules, migration data preservation expectations, and explicit no-fallback/no-compat-shim policy. Include handoff notes for Phase 40 golden queries, Phase 41 migration/import, Phase 42 implementation, and Phase 43 shadow run.</action>
  <verify>
    <automated>test -s docs/surrealdb-native-retrieval-contract.md</automated>
    <automated>test -s .planning/phases/39-surrealdb-native-retrieval-contract/39-RETRIEVAL-CONTRACT.md</automated>
    <automated>rg -n "baseline|compat|fallback|migration|embeddings|graph|feedback|cursor|checkpoint|improvement|harmless reorder|regression|unclear|Phase 40|Phase 41|Phase 42|Phase 43" docs/surrealdb-native-retrieval-contract.md .planning/phases/39-surrealdb-native-retrieval-contract/39-RETRIEVAL-CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - Docs say exact old-stack rank parity is not the product goal.
    - Docs say old-stack output is a baseline/evaluator only.
    - Docs include migration-preservation expectations for existing stored data.
    - Docs forbid runtime fallback backends and productized compatibility shims after cutover.
    - Docs state what later phases must consume from the contract.
  </acceptance_criteria>
  <done>Human-readable contract and phase-local handoff are written.</done>
</task>

</tasks>

<verification>
Run:

```bash
cd backend && uv run pytest tests/search/test_surreal_contract.py -q
cd backend && uv run python -m py_compile src/dotmd/search/surreal_contract.py
rg -n "baseline|fallback|compat|migration|regression|unclear" docs/surrealdb-native-retrieval-contract.md .planning/phases/39-surrealdb-native-retrieval-contract/39-RETRIEVAL-CONTRACT.md
```
</verification>

<success_criteria>
- `backend/src/dotmd/search/surreal_contract.py` exists and exports the contract vocabulary.
- `backend/tests/search/test_surreal_contract.py` passes.
- `docs/surrealdb-native-retrieval-contract.md` states the SurrealDB-native retrieval contract.
- `39-RETRIEVAL-CONTRACT.md` hands off explicit constraints to Phases 40-43.
- The plan preserves the user's data migration requirement instead of assuming clean-room recomputation.
</success_criteria>
