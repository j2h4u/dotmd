---
phase: 06-docker-integration-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /opt/docker/dotmd/docker-compose.yml
autonomous: false
requirements:
  - GRAPH-04
  - GRAPH-05

must_haves:
  truths:
    - "dotmd container resolves hostname 'falkordb' to the FalkorDB container IP on graphiti_default network"
    - "dotmd status reports graph_backend: falkordb with entity and edge counts after re-index"
    - "dotmd search --mode hybrid returns results with 'graph' in matched_engines"
    - "dotmd serve + curl /search returns graph-enriched results (concurrent CLI + API works)"
  artifacts:
    - path: "/opt/docker/dotmd/docker-compose.yml"
      provides: "Production compose with graphiti_default network and FalkorDB env vars"
      contains: "graphiti_default"
  key_links:
    - from: "/opt/docker/dotmd/docker-compose.yml"
      to: "graphiti_default network"
      via: "external network declaration"
      pattern: "name: graphiti_default"
    - from: "/opt/docker/dotmd/docker-compose.yml"
      to: "FalkorDB container"
      via: "DOTMD_FALKORDB_URL env var"
      pattern: "redis://falkordb:6379"
---

<objective>
Connect the dotmd production Docker container to FalkorDB and validate the full pipeline with a re-index.

Purpose: Complete the FalkorDB migration by wiring Docker networking so the dotmd container can reach FalkorDB on the shared `graphiti_default` network, then populate the graph via a full re-index.

Output: Updated docker-compose.yml with FalkorDB connectivity, rebuilt Docker image, validated hybrid search with graph results from FalkorDB.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-docker-integration-migration/06-CONTEXT.md
@.planning/phases/06-docker-integration-migration/06-RESEARCH.md
@.planning/phases/04-falkordb-adapter-config/04-02-SUMMARY.md

<interfaces>
<!-- Production compose file — the ONLY file being modified -->
<!-- Current state (read from /opt/docker/dotmd/docker-compose.yml): -->

```yaml
services:
  api:
    build:
      context: /home/j2h4u/repos/j2h4u/dotmd/backend
    ports:
      - "127.0.0.1:8321:8000"
    volumes:
      - /srv/knowledgebase/voicenotes:/mnt/voicenotes:ro
      - /home/j2h4u:/mnt/home:ro
      - dotmd-index:/dotmd-index
      - dotmd-hf-models:/root/.cache/huggingface
    environment:
      - DOTMD_DATA_DIR=/mnt
      - DOTMD_INDEX_DIR=/dotmd-index
      - DOTMD_EMBEDDING_URL=http://embeddings:80
      - DOTMD_EMBEDDING_DIM=1024
      - DOTMD_EXTRACT_DEPTH=ner
    env_file:
      - /home/j2h4u/.secrets/huggingface.env
    networks:
      - default
      - embeddings
    command: ["serve", "--host", "0.0.0.0"]

volumes:
  dotmd-index:
  dotmd-hf-models:

networks:
  embeddings:
    external: true
    name: embeddings_default
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FalkorDB network and env vars to production compose</name>
  <read_first>
    - /opt/docker/dotmd/docker-compose.yml (current production state — MUST read before editing)
    - /opt/docker/graphiti/docker-compose.yml (verify FalkorDB service name is `falkordb`)
  </read_first>
  <files>/opt/docker/dotmd/docker-compose.yml</files>
  <action>
Edit `/opt/docker/dotmd/docker-compose.yml` to add FalkorDB connectivity. The target state is:

1. **Add three FalkorDB env vars to the `api` service `environment:` block** (per D-03, D-04 — keep all existing vars unchanged, append new ones after `DOTMD_EXTRACT_DEPTH=ner`):
   - `DOTMD_GRAPH_BACKEND=falkordb`
   - `DOTMD_FALKORDB_URL=redis://falkordb:6379`
   - `DOTMD_FALKORDB_GRAPH_NAME=dotmd`

2. **Add `graphiti` to the `api` service `networks:` list** (per D-01 — follows same pattern as existing `embeddings`):
   ```yaml
   networks:
     - default
     - embeddings
     - graphiti
   ```

3. **Add `graphiti` external network declaration** in the top-level `networks:` section (per D-01, D-02 — identical pattern to `embeddings`):
   ```yaml
   networks:
     embeddings:
       external: true
       name: embeddings_default
     graphiti:
       external: true
       name: graphiti_default
   ```

The complete target file:
```yaml
services:
  api:
    build:
      context: /home/j2h4u/repos/j2h4u/dotmd/backend
    ports:
      - "127.0.0.1:8321:8000"
    volumes:
      - /srv/knowledgebase/voicenotes:/mnt/voicenotes:ro
      - /home/j2h4u:/mnt/home:ro
      - dotmd-index:/dotmd-index
      - dotmd-hf-models:/root/.cache/huggingface
    environment:
      - DOTMD_DATA_DIR=/mnt
      - DOTMD_INDEX_DIR=/dotmd-index
      - DOTMD_EMBEDDING_URL=http://embeddings:80
      - DOTMD_EMBEDDING_DIM=1024
      - DOTMD_EXTRACT_DEPTH=ner
      - DOTMD_GRAPH_BACKEND=falkordb
      - DOTMD_FALKORDB_URL=redis://falkordb:6379
      - DOTMD_FALKORDB_GRAPH_NAME=dotmd
    env_file:
      - /home/j2h4u/.secrets/huggingface.env
    networks:
      - default
      - embeddings
      - graphiti
    command: ["serve", "--host", "0.0.0.0"]

volumes:
  dotmd-index:
  dotmd-hf-models:

networks:
  embeddings:
    external: true
    name: embeddings_default
  graphiti:
    external: true
    name: graphiti_default
```

IMPORTANT: Do NOT change any existing values (ports, volumes, existing env vars, env_file, command, build context). Only ADD the three new env vars, add `graphiti` to the service networks list, and add the `graphiti` external network declaration.
  </action>
  <verify>
    <automated>grep -c "DOTMD_GRAPH_BACKEND=falkordb" /opt/docker/dotmd/docker-compose.yml && grep -c "DOTMD_FALKORDB_URL=redis://falkordb:6379" /opt/docker/dotmd/docker-compose.yml && grep -c "DOTMD_FALKORDB_GRAPH_NAME=dotmd" /opt/docker/dotmd/docker-compose.yml && grep -c "graphiti_default" /opt/docker/dotmd/docker-compose.yml && grep -c "DOTMD_EMBEDDING_URL=http://embeddings:80" /opt/docker/dotmd/docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - `/opt/docker/dotmd/docker-compose.yml` contains `DOTMD_GRAPH_BACKEND=falkordb`
    - `/opt/docker/dotmd/docker-compose.yml` contains `DOTMD_FALKORDB_URL=redis://falkordb:6379`
    - `/opt/docker/dotmd/docker-compose.yml` contains `DOTMD_FALKORDB_GRAPH_NAME=dotmd`
    - `/opt/docker/dotmd/docker-compose.yml` contains `name: graphiti_default`
    - `/opt/docker/dotmd/docker-compose.yml` still contains `DOTMD_EMBEDDING_URL=http://embeddings:80` (no regression)
    - `/opt/docker/dotmd/docker-compose.yml` still contains `DOTMD_EXTRACT_DEPTH=ner` (no regression)
    - `docker compose -f /opt/docker/dotmd/docker-compose.yml config` validates without errors
  </acceptance_criteria>
  <done>Production compose file has FalkorDB env vars and graphiti_default external network, validates with `docker compose config`</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild Docker image and verify FalkorDB connectivity</name>
  <read_first>
    - /opt/docker/dotmd/docker-compose.yml (verify Task 1 changes are in place)
    - /home/j2h4u/repos/j2h4u/dotmd/backend/pyproject.toml (confirm falkordb dependency exists)
  </read_first>
  <files>/opt/docker/dotmd/docker-compose.yml</files>
  <action>
Run the following commands sequentially to rebuild the image and verify FalkorDB is reachable:

1. **Rebuild the Docker image** (MANDATORY per research — current image was built 2026-03-23, before Phase 4 added the `falkordb` Python dependency):
   ```bash
   cd /opt/docker/dotmd && docker compose build
   ```
   This will invalidate the pip install layer because `pyproject.toml` changed (falkordb added in Phase 4). The PyTorch base layer is cached.

2. **Stop the current API service** (per D-06 — avoid concurrent access to file-based indexes):
   ```bash
   cd /opt/docker/dotmd && docker compose stop api
   ```

3. **Start the API service with the new image** to verify FalkorDB connectivity:
   ```bash
   cd /opt/docker/dotmd && docker compose up -d api
   ```

4. **Verify the falkordb Python package is installed** in the rebuilt image:
   ```bash
   docker exec dotmd-api-1 pip show falkordb
   ```
   Expected: Shows `falkordb` package info with version >= 1.6.0

5. **Verify DNS resolution to FalkorDB container**:
   ```bash
   docker exec dotmd-api-1 getent hosts falkordb
   ```
   Expected: Returns an IP address (e.g., `172.25.0.2  falkordb`)

6. **Verify dotmd status reports FalkorDB backend**:
   ```bash
   docker exec dotmd-api-1 dotmd status
   ```
   Expected output includes: `Graph: falkordb @ redis://falkordb:6379` (entity/edge counts will be 0 until re-index)

If step 4 fails with `ModuleNotFoundError`: the image wasn't rebuilt properly — rerun `docker compose build --no-cache`.
If step 5 fails with DNS error: verify `graphiti_default` network exists (`docker network ls | grep graphiti`) and FalkorDB is running (`docker ps | grep falkordb`).
If step 6 shows connection error: verify FalkorDB container is running and port 6379 is accessible on the shared network.

NOTE: Do NOT run the full re-index here. The re-index is an overnight operation (~59 min) and will be initiated by the operator in the next task.
  </action>
  <verify>
    <automated>docker exec dotmd-api-1 pip show falkordb 2>&1 | grep -c "Version:" && docker exec dotmd-api-1 getent hosts falkordb 2>&1 | grep -c "falkordb"</automated>
  </verify>
  <acceptance_criteria>
    - `docker exec dotmd-api-1 pip show falkordb` outputs a Version line (package installed)
    - `docker exec dotmd-api-1 getent hosts falkordb` resolves to an IP address
    - `docker exec dotmd-api-1 dotmd status` includes "falkordb" in output
    - `docker ps` shows `dotmd-api-1` running
  </acceptance_criteria>
  <done>Docker image rebuilt with falkordb dependency, container running on graphiti_default network, FalkorDB hostname resolvable, dotmd status reports falkordb backend</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Overnight re-index and full validation</name>
  <files></files>
  <action>
Operator runs overnight re-index and validates FalkorDB graph population. This is a human-action checkpoint because the re-index takes ~59 min and cannot be run by the executor within a session.

What was built: Docker compose updated with FalkorDB networking and env vars (Task 1). Image rebuilt with falkordb Python package and verified connectivity to FalkorDB container (Task 2). The application can now reach FalkorDB but the graph is empty — a full re-index is needed to populate it.

Step A — Run overnight re-index (~59 min):

```bash
# 1. Stop the running API service
cd /opt/docker/dotmd && docker compose stop api

# 2. Run full re-index (will take ~59 min)
docker compose run --rm api index --force /mnt

# 3. After re-index completes, start the API service
docker compose up -d api
```

Step B — Validate (after re-index completes and API is back up):

Check 1 — Status shows FalkorDB with data:
```bash
docker exec dotmd-api-1 dotmd status
```
Expected: `graph_backend: falkordb`, entity count > 0, edge count > 0

Check 2 — Hybrid search includes graph results:
```bash
docker exec dotmd-api-1 dotmd search --mode hybrid "test query"
```
Expected: At least some results have `graph` in `matched_engines`

Check 3 — API serves results (concurrent access works):
```bash
curl -s "http://localhost:8321/search?q=test&mode=hybrid" | python3 -m json.tool | head -20
```
Expected: HTTP 200 with search results

Check 4 — FalkorDB graph has data (direct check):
```bash
docker exec graphiti-falkordb-1 redis-cli GRAPH.QUERY dotmd "MATCH (n) RETURN count(n)"
```
Expected: Count > 0 (should be ~3000+ entities based on previous index baseline of 3,456)
  </action>
  <verify>
    <automated>docker exec dotmd-api-1 dotmd status 2>&1 | grep -c "falkordb" && curl -s -o /dev/null -w "%{http_code}" "http://localhost:8321/search?q=test&mode=hybrid" | grep -c "200"</automated>
  </verify>
  <acceptance_criteria>
    - `docker exec dotmd-api-1 dotmd status` shows `falkordb` backend with entity count > 0
    - `docker exec dotmd-api-1 dotmd search --mode hybrid "test query"` returns results with `graph` in matched_engines
    - `curl http://localhost:8321/search?q=test&mode=hybrid` returns HTTP 200
    - `docker exec graphiti-falkordb-1 redis-cli GRAPH.QUERY dotmd "MATCH (n) RETURN count(n)"` returns count > 0
  </acceptance_criteria>
  <done>FalkorDB graph populated with entities and edges, hybrid search returns graph results, API serves concurrent requests</done>
</task>

</tasks>

<verification>
Phase 6 is complete when ALL of these are true:
1. `/opt/docker/dotmd/docker-compose.yml` contains `graphiti_default` external network and three FalkorDB env vars
2. Docker image is rebuilt with `falkordb` Python package installed
3. `dotmd-api-1` container resolves `falkordb` hostname on `graphiti_default` network
4. Full re-index populates FalkorDB graph (entity count > 0)
5. `dotmd search --mode hybrid` returns results with `graph` in matched_engines
6. `curl http://localhost:8321/search` returns HTTP 200 with results (API concurrent access works)
</verification>

<success_criteria>
- docker-compose.yml has `DOTMD_GRAPH_BACKEND=falkordb`, `DOTMD_FALKORDB_URL=redis://falkordb:6379`, `DOTMD_FALKORDB_GRAPH_NAME=dotmd`
- docker-compose.yml has `graphiti_default` external network and `graphiti` in api service networks
- All existing env vars unchanged (no regression)
- FalkorDB graph populated with > 0 entities and edges after re-index
- Hybrid search returns graph-sourced results
- API endpoint accessible at port 8321
</success_criteria>

<output>
After completion, create `.planning/phases/06-docker-integration-migration/06-01-SUMMARY.md`
</output>
