---
phase: 35-filesystem-unified-source-adapter
plan: "01"
wave: 1
depends_on: []
type: execute
autonomous: true
files_modified:
  - backend/src/dotmd/ingestion/source.py
  - backend/src/dotmd/ingestion/pipeline.py
  - backend/tests/ingestion/test_source_filesystem.py
requirements_addressed:
  - FS-02
  - FS-03
---

<objective>
Rename `FilesystemMarkdownSourceAdapter._from_file_info` to `document_for_file_info`
(public), update all callers (internal self-calls + pipeline call site), and update
the `_RecordingLifecycleAdapter` test double to match.

After this plan, no `_from_file_info` references exist in production code
(`backend/src/`), and the existing test suite passes unchanged.
</objective>

<context>
@.planning/phases/35-filesystem-unified-source-adapter/35-CONTEXT.md
@.planning/phases/35-filesystem-unified-source-adapter/35-RESEARCH.md
@backend/src/dotmd/ingestion/source.py
@backend/src/dotmd/ingestion/pipeline.py
@backend/tests/ingestion/test_source_filesystem.py
</context>

<tasks>

<task id="35-01-01">
<title>Rename _from_file_info to document_for_file_info in source.py</title>

<read_first>
- `backend/src/dotmd/ingestion/source.py` — current definition of `_from_file_info` and all call sites within the class
</read_first>

<action>
In `backend/src/dotmd/ingestion/source.py`, apply the following changes:

1. Rename the method definition:
   - Old: `def _from_file_info(self, file_info: FileInfo) -> SourceDocument:`
   - New: `def document_for_file_info(self, file_info: FileInfo) -> SourceDocument:`

2. Update the two internal call sites within `FilesystemMarkdownSourceAdapter`:
   - In `discover()`: `self._from_file_info(file_info)` → `self.document_for_file_info(file_info)`
   - In `discover_multi()`: `self._from_file_info(file_info)` → `self.document_for_file_info(file_info)`

No other changes to this file. `SourceAdapterProtocol` stays at `discover` +
`discover_multi` only. `source_document_to_file_info()` is untouched.
</action>

<acceptance_criteria>
- `backend/src/dotmd/ingestion/source.py` contains `def document_for_file_info(self, file_info: FileInfo) -> SourceDocument:`
- `backend/src/dotmd/ingestion/source.py` does NOT contain `_from_file_info`
- `grep -n "_from_file_info" backend/src/dotmd/ingestion/source.py` returns no output
- `grep -n "document_for_file_info" backend/src/dotmd/ingestion/source.py` returns at least 3 lines (definition + 2 internal callers)
</acceptance_criteria>

<automated>cd backend && python -m pytest tests/ingestion/test_source_filesystem.py tests/ingestion/test_source_lifecycle.py -q 2>&1 | tail -5</automated>
</task>

<task id="35-01-02">
<title>Update pipeline.py call site from _from_file_info to document_for_file_info</title>

<read_first>
- `backend/src/dotmd/ingestion/pipeline.py` — the `_source_document_for_file_info` method, specifically the line `bundle.source._from_file_info(file_info)` around line 1371
</read_first>

<action>
In `backend/src/dotmd/ingestion/pipeline.py`, inside the method
`_source_document_for_file_info`, change:

```python
source_document = bundle.source._from_file_info(file_info)
```

to:

```python
source_document = bundle.source.document_for_file_info(file_info)
```

This is the only call site in `pipeline.py` that invokes the renamed method.
No other changes to `pipeline.py`. All `_filesystem_*` orchestration methods
stay in place unchanged.
</action>

<acceptance_criteria>
- `grep -n "_from_file_info" backend/src/dotmd/ingestion/pipeline.py` returns no output
- `grep -n "document_for_file_info" backend/src/dotmd/ingestion/pipeline.py` returns exactly 1 line (inside `_source_document_for_file_info`)
- `cd backend && python -m pytest tests/ingestion/test_source_filesystem.py::test_pipeline_source_document_for_file_info_uses_lifecycle_adapter -q` exits 0
</acceptance_criteria>

<automated>cd backend && python -m pytest tests/ingestion/test_source_filesystem.py::test_pipeline_source_document_for_file_info_uses_lifecycle_adapter -q 2>&1 | tail -5</automated>
</task>

<task id="35-01-03">
<title>Update _RecordingLifecycleAdapter test double in test_source_filesystem.py</title>

<read_first>
- `backend/tests/ingestion/test_source_filesystem.py` — the `_RecordingLifecycleAdapter` class (around line 250) and its `_from_file_info` override
</read_first>

<action>
In `backend/tests/ingestion/test_source_filesystem.py`, update the
`_RecordingLifecycleAdapter` class method override:

Old:
```python
def _from_file_info(self, file_info: FileInfo) -> SourceDocument:
    self.file_infos.append(file_info)
    return super()._from_file_info(file_info)
```

New:
```python
def document_for_file_info(self, file_info: FileInfo) -> SourceDocument:
    self.file_infos.append(file_info)
    return super().document_for_file_info(file_info)
```

No other changes. The test body relying on `adapter.file_infos` does not need
updating — only the method name in the recorder changes.
</action>

<acceptance_criteria>
- `grep -n "_from_file_info" backend/tests/ingestion/test_source_filesystem.py` returns no output
- `grep -n "document_for_file_info" backend/tests/ingestion/test_source_filesystem.py` returns at least 1 line (the recorder override)
- `cd backend && python -m pytest tests/ingestion/test_source_filesystem.py -q` exits 0 (all existing tests pass)
</acceptance_criteria>

<automated>cd backend && python -m pytest tests/ingestion/test_source_filesystem.py -q 2>&1 | tail -5</automated>
</task>

</tasks>

<verification>
## Verification

After all three tasks:

1. **No private method bypass** — `grep -rn "_from_file_info" backend/src/` returns no output
2. **Full source filesystem test suite passes** — `cd backend && python -m pytest tests/ingestion/test_source_filesystem.py tests/ingestion/test_source_lifecycle.py -q` exits 0
3. **Public method accessible** — `grep -n "def document_for_file_info" backend/src/dotmd/ingestion/source.py` returns exactly 1 line
4. **Pipeline call site updated** — `grep -n "bundle.source.document_for_file_info" backend/src/dotmd/ingestion/pipeline.py` returns exactly 1 line

### must_haves
- **truths:**
  - `FilesystemMarkdownSourceAdapter` has a public `document_for_file_info(file_info)` method
  - No `_from_file_info` exists anywhere in `backend/src/`
  - `bundle.source.document_for_file_info(file_info)` is the call site in `pipeline._source_document_for_file_info`
  - Existing test suite passes without modification to test logic
</verification>
