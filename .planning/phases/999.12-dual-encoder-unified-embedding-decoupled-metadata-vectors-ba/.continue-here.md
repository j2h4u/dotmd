---
context: phase
phase: 999.12-dual-encoder-unified-embedding-decoupled-metadata-vectors
task: deploy
status: ready_to_deploy
last_updated: 2026-04-27T14:38:25Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions.

- [ ] CONSTRAINT: wipe-on-deploy — запуск контейнера с новым кодом **немедленно уничтожает все текущие векторы** через schema version check. Бэкап обязателен до любого деплоя.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| deploy without backup | Schema version bump вайпает embedding_cache, vec_meta, vec_components, vec0 при старте контейнера. Откат невозможен без бэкапа. | blocking | Сначала borg backup, потом деплой |

<current_state>
Ветка `feature/dual-encoder-unified-embeddings` полностью готова к продакшену.

- Phase 999.12 выполнена: 3 плана, все верификации пройдены
- Code review + все фиксы применены
- Quality sweep завершён: 10 волн, 34 фикса, 189 тестов проходят
- Uncommitted changes: нет (только untracked .claude/, uv.lock — не нужны)

**Текущая ситуация (конец сессии):** обсудили архитектуру trickle (backlog → watch mode), убедились что всё корректно для ~300 файлов. Реиндексация займёт несколько часов, не дней.
</current_state>

<completed_work>
- Plan 01–03 фазы 999.12: dual-encoder архитектура, vec_components, meta_tracker, e_fused fusion, search_log, schema version migration
- Code review (999.12-REVIEW.md) — все findings зафиксированы
- Quality sweep waves 1–10:
  - Wave 1: 3 критических теста (silent skip, tautology, vague assert)
  - Wave 2: error handling criticals (except narrowing, partial wipe log → error)
  - Wave 3: stale docstring refs
  - Wave 4: 5 test improvements (chunk_count floor, vec0/vec_meta wipe checks, pipeline._conn)
  - Wave 5: error handling visibility (4 fix)
  - Wave 6: 4 рефакторинга имён (meta_diff, e_text_norm, query_instruction, _enrich_passthrough)
  - Wave 7: defense-in-depth (strategy sanitize, DROP TABLE validation)
  - Wave 8: observability (6 fix: debug noise, full path в DONE log, search entry log)
  - Wave 9: 4 docstring дополнения
  - Wave 10: удалены review attribution комментарии из pipeline.py
</completed_work>

<remaining_work>
1. **Borg backup** — обязательно перед деплоем (особенно /opt/docker/dotmd и dotmd_dotmd-index volume)
2. **Merge ветки** — `feature/dual-encoder-unified-embeddings` → `dev`
3. **Деплой** — `docker compose up -d dotmd` в /opt/docker/dotmd
4. **Мониторинг реиндексации** — логи trickle, ETA появляется после 3 файлов + 2 минуты:
   `docker logs -f dotmd | grep -E "\[.*\]|Backlog|ETA|chunks/hr"`
</remaining_work>

<decisions_made>
- **D-14 (wipe-on-deploy):** text_hash семантика изменилась: `hash(title+tags+body)` → `hash(body only)`. Старые векторы несовместимы, перестройка с нуля единственный путь.
- **~300 файлов → несколько часов:** оценка "несколько дней" в CONTEXT.md была для большого датасета. При ~300 файлах реиндексация займёт несколько часов на CPU TEI.
- **lstrip("\n") в parse_frontmatter:** frontmatter-разделитель whitespace уже нормализуется на строке 34 reader.py — изменение числа переносов строк после `---` не меняет chunk_checksum. Проблема была в другом.
- **trickle архитектура корректна:** backlog без watchdog → watch mode с hourly polling fallback. Для ~1 файла в день задержка до 1 часа приемлема.
</decisions_made>

<blockers>
Нет технических блокеров. Единственное требование перед деплоем — borg backup.
</blockers>

## Required Reading (in order)

1. `.planning/phases/999.12-dual-encoder-unified-embedding-decoupled-metadata-vectors-ba/999.12-CONTEXT.md` — архитектурные решения D-01 — D-15, особенно D-14 (wipe-on-deploy)
2. `/opt/docker/dotmd/AGENTS.md` — деплой процедура, env vars, volume mapping

## Infrastructure State

- **Branch**: `feature/dual-encoder-unified-embeddings` — готова, все тесты зелёные
- **Production container**: работает на старом коде (без dual-encoder)
- **TEI**: `embeddings` контейнер на порту 8088, работает
- **Volume**: `dotmd_dotmd-index` — будет вайпнут при первом старте нового кода
- **Borg backup**: требуется перед деплоем, скрипт: `~/scripts/cron/borg-backup.sh`

<context>
Ветка технически полностью готова. Прошли полный цикл: реализация → code review → quality sweep. Следующий шаг — чисто операционный: бэкап, деплой, мониторинг. Реиндексация ожидается несколько часов, trickle выведет ETA в логи после первых двух минут работы.
</context>

<next_action>
1. `~/scripts/cron/borg-backup.sh` — дождаться завершения
2. Смержить ветку: `git checkout dev && git merge feature/dual-encoder-unified-embeddings`
3. `cd /opt/docker/dotmd && docker compose up -d dotmd`
4. `docker logs -f dotmd | grep -E "\[.*\]|Backlog|ETA|DONE"` — наблюдать прогресс
</next_action>
