---
phase: 999.12
status: all_fixed
findings_in_scope: 10
fixed: 9
skipped: 1
iteration: 2
---

# Phase 999.12: Code Review Fix Report

**Applied:** 2026-04-27
**Scope:** all (Critical + Warning + Info)
**Tests after fixes:** 189 passed, 0 failed

---

## Iteration 1 — Critical + Warning (previous run)

### CR-01 + WR-01: WIPE_IN_PROGRESS sentinel + count() guard
**Commit:** `d2df460`

Two-sentinel crash-safe wipe in `_check_schema_version()`: `WIPE_IN_PROGRESS` written before any wipe step; `SCHEMA_VERSION` written last. Interrupted wipe detected and resumed on next startup. Added `vec_components.count()` guard for `stored_version=None` to distinguish fresh DBs from orphaned partial deployments.

### WR-02: No-chunks early-return — already correct
**Status:** SKIPPED — already fixed by wave-3 executor.

### WR-03: Explicit `conn.commit()` before vec0 write
**Commit:** `d43902c` — `_save_and_embed_chunks`

### WR-04: `delete_all()` once + `overwrite=False` in weight recompute loop
**Commit:** `c6457dc` — `_check_weights_changed`

### WR-05: `frontmatter or {}` guard
**Commit:** `88ca3bf` — `_build_file_meta_from_fileinfo`

---

## Iteration 2 — Info (this run)

### IN-01: Delete dead `enrich_with_title_and_tags`
**Commit:** `93c519e` — `content_handlers.py`

Function removed entirely. The ADR context it documented is already captured in the `_enrich_noop` docstring directly below it.

### IN-02: `DeprecationWarning` on legacy property aliases
**Commit:** `c382c89` — `config.py`

Added `import warnings` and `warnings.warn(..., DeprecationWarning, stacklevel=2)` to both `sqlite_vec_path` and `sqlite_path` properties. Callers will now see the warning in test output.

### IN-03 + IN-04: Module-level `import re`, `import tempfile`, `import json`
**Commit:** `d67e403` — `pipeline.py`

- Added `import re` and `import tempfile` to module-level imports
- Removed inline `import re` from `_model_to_table_suffix`
- Removed inline `import json` from `log_search` and `_rebuild_acronyms`
- Removed `import json as _json` from `_check_weights_changed`; uses module-level `json`

---

## Commit Log

| Commit | Finding | File |
|--------|---------|------|
| `88ca3bf` | WR-05 | pipeline.py — frontmatter None guard |
| `d43902c` | WR-03 | pipeline.py — conn.commit() before vec0 |
| `c6457dc` | WR-04 | pipeline.py — delete_all() + overwrite=False |
| `d2df460` | CR-01+WR-01 | pipeline.py — WIPE_IN_PROGRESS + count() guard |
| `93c519e` | IN-01 | content_handlers.py — delete dead function |
| `c382c89` | IN-02 | config.py — DeprecationWarning on aliases |
| `d67e403` | IN-03+IN-04 | pipeline.py — module-level imports |
