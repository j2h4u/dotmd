---
phase: 999.12-dual-encoder-unified-embedding-decoupled-metadata-vectors
plan: "02"
subsystem: ingestion-pipeline
tags: [dual-encoder, embedding, fusion, pipeline-refactor, meta-tracker, vec-components, schema-version]
dependency_graph:
  requires: ["999.12-01"]
  provides: ["dual-encoder-pipeline-core"]
  affects: ["backend/src/dotmd/ingestion/pipeline.py", "backend/src/dotmd/ingestion/content_handlers.py", "backend/src/dotmd/ingestion/reader.py"]
tech_stack:
  added: []
  patterns:
    - "two-phase transaction: TEI calls outside BEGIN block, writes inside BEGIN...COMMIT"
    - "_meta_entity_id() static method as single canonical path normalizer"
    - "three-layer e_text cache: VecComponentStore → embedding_cache → TEI"
    - "pure _embed_chunks: returns (e_text_vectors, text_hashes), no storage side effects"
key_files:
  created: []
  modified:
    - backend/src/dotmd/ingestion/pipeline.py
    - backend/src/dotmd/ingestion/content_handlers.py
    - backend/src/dotmd/ingestion/reader.py
    - backend/src/dotmd/ingestion/file_tracker.py
    - backend/src/dotmd/ingestion/migrate_fingerprints_to_blake3.py
decisions:
  - "embed_checksum removed from reader.py (replaced by meta_checksum); no backward compat needed per project rules"
  - "_embed_chunks is PURE: no VecComponentStore.store calls inside; all callers own storage"
  - "lookup_embeddings_by_text_hash removed from _embed_chunks: post-999.12 vec0 stores e_fused not e_text (double-fusion risk)"
  - "_meta_entity_id() centralizes all meta component entity_id normalization (Codex HIGH Cycle 3)"
  - "schema version wipe clears all 7 state components atomically; crash before COMMIT triggers re-wipe"
  - "_check_weights_changed skips sentinel update on partial recompute (Codex MEDIUM)"
metrics:
  duration: "~45 minutes"
  completed: "2026-04-27"
  tasks_completed: 5
  files_modified: 5
---

# Phase 999.12 Plan 02: Pipeline Refactor — embed_tracker → meta_tracker, e_text storage, metadata-only fast path

Dual-encoder pipeline core: replaced embed_tracker with meta_tracker, rewrote _embed_chunks as a pure three-layer cache, added _index_file_embed with explicit body/metadata branching and atomic transaction, added _fuse_vectors/_embed_meta_component helpers, and implemented schema version wipe with weight-change detection.

## Tasks Completed

| Task | Name | Commit | Key Changes |
|------|------|--------|-------------|
| 1 | Verify FTS5 path, remove embed enrichment from registry | a19f753 | _enrich_noop added; all ContentHandler entries use noop for embedding path |
| 2 | Replace embed_tracker with meta_tracker; add VecComponentStore; fix _embed_chunks | 3c86d59 | embed_checksum removed; meta_checksum imported; VecComponentStore initialized; _embed_chunks pure 3-layer cache; helpers added |
| 3 | Add _index_file_embed with call chain, branching, transaction | 58004c0 | _index_file_embed() with body_changed/metadata-only paths; explicit BEGIN/COMMIT/ROLLBACK; TEI calls outside tx |
| 4 | Update bulk run(), _save_and_embed_chunks(), _embed_existing_chunks(), reindex_vectors() | 76e4615 | reindex_vectors groups by file + batch e_meta; _save_and_embed_chunks fuses per file; _embed_existing_chunks model_switch parameter |
| 5 | Add schema version wipe-on-deploy and weight-change detection | 8718a7c | SCHEMA_VERSION="2"; _check_schema_version atomic wipe; _check_weights_changed with empty-store guard + partial-recompute guard |

## Deviations from Plan

### Auto-fixed Issues

None beyond minor code structure adaptations:

**1. [Rule 1 - Bug] strategy_suffix variable reference in __init__**
- Found during: Task 2 Step 3 (VecComponentStore table name construction)
- Issue: Plan referenced `strategy_suffix` variable which doesn't exist in __init__; naming convention uses plain `strategy` string
- Fix: Used `f"vec_components_{strategy}{model_suffix}"` instead of the templated form
- Files modified: pipeline.py

**2. [Rule 2 - Missing handling] drop_vectors/drop_chunks: meta_fingerprints and vec_components tables**
- Found during: Task 2
- Issue: drop_vectors still cleared embed_fingerprints (old name); drop_chunks cascade didn't include meta_fingerprints or vec_components prefix
- Fix: Updated both methods to handle meta_fingerprints, vec_components, and retain legacy embed_fingerprints prefix for backward compat
- Files modified: pipeline.py

**3. [Rule 1 - Bug] index_file Phase 2 FTS5 refresh in metadata-only path**
- Found during: Task 3
- Issue: After delegating to _index_file_embed(), the FTS5+graph refresh for metadata_only path needed to be retained in index_file() (not inside _index_file_embed() which handles only vectors)
- Fix: Kept FTS5 + frontmatter_to_graph calls in index_file() for the metadata_only path after _index_file_embed() returns
- Files modified: pipeline.py

**4. [Rule 2 - Missing handling] _save_and_embed_chunks needs to handle chunks with empty file_paths**
- Found during: Task 4
- Issue: Chunks returned by get_all_chunks() have empty file_paths (Phase 16 design); _save_and_embed_chunks needed fallback for chunks without file_paths[0]
- Fix: Added fallback_path = files[0].path when chunk.file_paths is empty
- Files modified: pipeline.py

## Known Stubs

None. All implemented functionality is wired end-to-end.

## Threat Flags

None. No new network endpoints, auth paths, or trust boundary changes introduced.

## Self-Check: PASSED

All 5 commits present:
- a19f753 feat(999.12-02): noop enrich in embedding path
- 3c86d59 feat(999.12-02): replace embed_tracker with meta_tracker
- 58004c0 feat(999.12-02): add _index_file_embed
- 76e4615 feat(999.12-02): update bulk paths
- 8718a7c feat(999.12-02): schema version wipe and weight-change detection

Key files verified present:
- backend/src/dotmd/ingestion/pipeline.py (modified)
- backend/src/dotmd/ingestion/content_handlers.py (modified)
- backend/src/dotmd/ingestion/reader.py (modified)

All plan verification checks pass: import OK, enrich noop OK, embed_checksum removed, reader checksum API OK, _meta_entity_id consistency OK, _embed_chunks purity OK, _fuse_vectors dimension check OK, _index_file_embed transaction OK, _embed_existing_chunks model_switch OK, _check_schema_version atomicity OK, _check_weights_changed guards OK.
