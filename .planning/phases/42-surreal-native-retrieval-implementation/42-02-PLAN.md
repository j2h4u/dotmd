---
phase: 42-surreal-native-retrieval-implementation
plan: 02
type: tdd
wave: 2
depends_on:
  - 42-01
files_modified:
  - backend/src/dotmd/search/surreal_fts.py
  - backend/src/dotmd/search/surreal_vector.py
  - backend/tests/search/test_surreal_native_fts.py
  - backend/tests/search/test_surreal_native_vector.py
autonomous: true
requirements:
  - SURR-SEARCH-01
  - SURR-SEARCH-02
must_haves:
  truths:
    - "SURR-SEARCH-01: A Surreal-backed full-text engine returns chunk-id scores from separate title, tags, and text BM25 predicates with weighted composition."
    - "SURR-SEARCH-02: A Surreal-backed vector engine encodes queries and retrieves nearest chunks through HNSW KNN queries rather than Python embedding scans."
    - "SURR-SEARCH-02: Vector retrieval enforces the Phase 42 single-active-model/dimension precondition and bounded top-k/HNSW ef contract before querying Surreal."
    - "Both engines implement SearchEngineProtocol and fail soft with [] on empty query or operational Surreal errors."
  artifacts:
    - path: "backend/src/dotmd/search/surreal_fts.py"
      provides: "Weighted Surreal BM25 SearchEngineProtocol implementation"
      exports: ["SurrealFTSSearchEngine"]
    - path: "backend/src/dotmd/search/surreal_vector.py"
      provides: "HNSW Surreal vector SearchEngineProtocol implementation"
      exports: ["SurrealVectorSearchEngine"]
  key_links:
    - from: "backend/src/dotmd/search/surreal_fts.py"
      to: "backend/src/dotmd/storage/surreal_schema.py"
      via: "uses the fields and index names created in 42-01"
      pattern: "title.*tags_text.*text"
    - from: "backend/src/dotmd/search/surreal_vector.py"
      to: "backend/src/dotmd/search/semantic.py"
      via: "preserves TEI/local query encoding behavior before Surreal HNSW lookup"
      pattern: "query:"
    - from: "backend/tests/search/test_surreal_native_vector.py"
      to: "backend/tests/fixtures/surreal_native.py"
      via: "imports the Plan 42-01-owned embedded Surreal fixture for mandatory real HNSW assertions"
      pattern: "isolated_surreal_connection"
---

<objective>
Implement Surreal-native full-text and vector retrieval engines behind the existing `SearchEngineProtocol`.

Purpose: Phase 38 proxy logic used Python-side scans and old-stack comparison helpers. This plan replaces the lexical and vector hot paths with real Surreal BM25 and HNSW queries while keeping the existing engine return contract.
Output: `SurrealFTSSearchEngine`, `SurrealVectorSearchEngine`, and focused tests for weighted lexical and indexed vector retrieval.
</objective>

<execution_context>
@/home/j2h4u/.codex/gsd-core/workflows/execute-plan.md
@/home/j2h4u/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-surreal-native-retrieval-implementation/42-RESEARCH.md
@.planning/phases/42-surreal-native-retrieval-implementation/42-PATTERNS.md
@.planning/phases/42-surreal-native-retrieval-implementation/42-01-SUMMARY.md
@backend/src/dotmd/search/base.py
@backend/src/dotmd/search/fts5.py
@backend/src/dotmd/search/semantic.py
@backend/src/dotmd/storage/surreal.py
@backend/src/dotmd/storage/surreal_schema.py
@backend/tests/fixtures/surreal_native.py
</context>

<review_feedback>
Cycle 1 convergence notes:

| Concern | Severity | Plan change |
|---------|----------|-------------|
| HNSW model/dimension ambiguity | MEDIUM | `SurrealVectorSearchEngine` must enforce the 42-01 single-active-model/dimension-uniformity precondition before HNSW queries and must filter by the configured embedding model through variables. |
| Fake-only FTS/vector tests | MEDIUM | Tests must combine fake-connection statement assertions with mandatory embedded `surrealkv://` assertions imported from `backend/tests/fixtures/surreal_native.py`; no engine can pass with fake-only tests. |
| `hnsw_ef` / top-k bounds | LOW | Engine validation uses the shared 42-01 bounds: `1 <= top_k <= 100`, `10 <= hnsw_ef <= 400`, `hnsw_ef >= top_k`, default `hnsw_ef=40`. |
| FTS5 wildcard ambiguity | LOW | Surreal FTS preprocessing may drop empty or unsafe predicate input, but it must not append FTS5 prefix wildcards or interpolate user text into SurrealQL. |
</review_feedback>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED tests for Surreal BM25 and HNSW engines</name>
  <files>backend/tests/search/test_surreal_native_fts.py, backend/tests/search/test_surreal_native_vector.py</files>
  <behavior>
    - Test 1: `SurrealFTSSearchEngine.search()` returns [] for blank or punctuation-only queries and does not call Surreal.
    - Test 2: `SurrealFTSSearchEngine.search()` issues one fixed SurrealQL statement using numbered title, tags, and text predicates, query variables for user text, title weight 5, tags weight 3, text weight 1, deterministic score ordering, and chunk-id tuple output.
    - Test 3: the FTS engine logs and returns [] on Surreal parse/runtime errors without raising through `DotMDService`.
    - Test 4: a mandatory embedded Surreal assertion inserts indexed chunk rows with title, tags_text, and text, applies the 42-01 retrieval schema through `backend/tests/fixtures/surreal_native.py`, runs `SurrealFTSSearchEngine.search()`, and proves a known tag/title match returns a non-empty scored chunk list.
    - Test 5: `SurrealVectorSearchEngine.search()` preserves `SemanticSearchEngine` query normalization: query instruction first, then E5 query prefix when enabled, then raw query when disabled.
    - Test 6: vector search uses a Surreal HNSW KNN statement with `embedding <|top_k,ef|> $qvec`, model filtering, cosine score projection, and no `scan_table()` use in the search hot path.
    - Test 7: vector search rejects `top_k` outside 1..100, `hnsw_ef` outside 10..400, and `hnsw_ef < top_k` before statement construction; default `hnsw_ef` is 40.
    - Test 8: vector search fails closed when target embeddings contain multiple active `embedding_model` values or a vector dimension different from the configured `embedding_dimension`.
    - Test 9: a mandatory embedded Surreal assertion inserts two 3-dimensional vectors for one embedding model, runs the real HNSW query with a mocked encoded query vector, and proves the nearest chunk is returned without any `scan_table()` call.
    - Test 10: vector search applies the existing relative `score_floor` behavior and returns [] on encode or Surreal errors only when the existing semantic engine would fail soft.
  </behavior>
  <action>Write failing tests that use lightweight fake connections for statement/variable assertions and mandatory isolated embedded Surreal assertions through the Plan 42-01 fixture. The embedded FTS test must apply the retrieval schema, insert at least one row with non-empty `tags_text`, and assert a real Surreal BM25 result. The embedded vector test must apply the retrieval schema, insert only one embedding model with dimension 3, monkeypatch query encoding to return a known 3-float vector, and assert a real HNSW result. Monkeypatch `scan_table` to raise in vector tests so the old Phase 38 proxy path cannot satisfy SURR-SEARCH-02. Keep TEI calls mocked through `encode()` or `_encode_via_tei` so tests stay local and fast. Import `backend/tests/fixtures/surreal_native.py` but do not modify it in this plan.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/search/test_surreal_native_fts.py tests/search/test_surreal_native_vector.py -q</automated>
  </verify>
  <done>The tests fail only because `SurrealFTSSearchEngine`, `SurrealVectorSearchEngine`, embedded retrieval execution, and the shared HNSW validation contract are not implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Surreal full-text and vector search engines</name>
  <files>backend/src/dotmd/search/surreal_fts.py, backend/src/dotmd/search/surreal_vector.py, backend/tests/search/test_surreal_native_fts.py, backend/tests/search/test_surreal_native_vector.py</files>
  <behavior>
    - Full-text search composes weighted BM25 scores from separate `title`, `tags_text`, and `text` predicates and returns `list[tuple[str, float]]` sorted by weighted score descending with chunk-id tie-breaks.
    - Vector search encodes the query exactly like the current semantic engine and sends the embedding to Surreal HNSW lookup with bounded integer top-k and ef values.
    - Vector search enforces one active embedding model and a uniform vector dimension matching the configured `embedding_dimension` before querying HNSW.
    - Both modules use `SurrealConnection.query()` with query variables for user text, vectors, and model names; fixed schema identifiers are constants.
    - Neither module imports old FTS5/sqlite-vec/Falkor implementations except in tests for behavior comparison.
  </behavior>
  <action>Create `surreal_fts.py` and `surreal_vector.py` as small engine modules following the import/logger/style patterns from `fts5.py` and `semantic.py`. Use `SurrealConnection` as the injected dependency. In the FTS engine, sanitize query text only enough to avoid empty predicates and pass the actual query as `$query`; do not append FTS5-style wildcard suffixes and do not interpolate user text. In the vector engine, reuse the semantic query-prefix and query-instruction semantics, validate `1 <= top_k <= 100`, `10 <= hnsw_ef <= 400`, and `hnsw_ef >= top_k`, then query `embeddings` with the HNSW operator form verified during planning: `embedding <|top_k,ef|> $qvec`, where `top_k`, `hnsw_ef`, `$qvec`, and `$embedding_model` are variables or validated data values. Before the first vector query for a connection/model, validate the Phase 42 precondition: exactly one active embedding model is present for the target retrieval index and every non-empty vector length equals the configured `embedding_dimension`; return [] with a warning when the precondition fails. Keep all engine errors fail-soft and log warning diagnostics with bounded query length, not full query text.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/search/test_surreal_native_fts.py tests/search/test_surreal_native_vector.py -q</automated>
  </verify>
  <done>Focused FTS/vector tests pass, both engines satisfy `SearchEngineProtocol`, and the Phase 38 scan-based vector path is no longer capable of satisfying the vector tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query -> Surreal full-text predicates | Query text enters SurrealQL through variables only. |
| query embedding -> Surreal HNSW lookup | User-derived vector enters KNN query as a variable. |
| Surreal result rows -> engine tuple output | Database rows are normalized before reaching service fusion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02-01 | Tampering | `SurrealFTSSearchEngine.search()` | mitigate | Keep table/field/index identifiers fixed and pass user query as a Surreal variable; tests assert hostile query text is inert. |
| T-42-02-02 | Denial of Service | FTS and vector top-k/ef parameters | mitigate | Clamp or validate top-k and HNSW ef as bounded positive integers before statement construction. |
| T-42-02-03 | Information Disclosure | engine warning logs | mitigate | Log query length and error class, not raw query text or result snippets. |
| T-42-02-04 | Tampering | vector lookup model filter | mitigate | Filter by configured embedding model through variables so mixed-model embeddings cannot silently cross-contaminate results. |
| T-42-02-SC | Tampering | package installs | accept | No package install or SurrealDB upgrade is planned; the existing SDK/runtime is used. |
</threat_model>

<verification>
Task gate:

`cd backend && uv run pytest tests/search/test_surreal_native_fts.py tests/search/test_surreal_native_vector.py -q`

Lint gate:

`cd backend && uv run ruff check src/dotmd/search/surreal_fts.py src/dotmd/search/surreal_vector.py tests/search/test_surreal_native_fts.py tests/search/test_surreal_native_vector.py`
</verification>

<success_criteria>
Plan 42-02 is complete when lexical and vector searches are implemented as Surreal-native engines with mandatory embedded retrieval assertions, no Python full-table embedding scan, no old FTS5 dependency, concrete HNSW bounds, enforced single-model/dimension preconditions, no runtime-upgrade assumption, and the same tuple return contract used by the existing fusion layer.
</success_criteria>

<output>
Create `.planning/phases/42-surreal-native-retrieval-implementation/42-02-SUMMARY.md` when done.
</output>
