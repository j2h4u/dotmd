---
phase: 42-surreal-native-retrieval-implementation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/dotmd/storage/surreal_schema.py
  - backend/src/dotmd/ingestion/migrate_surreal.py
  - backend/tests/fixtures/surreal_native.py
  - backend/tests/storage/test_surreal_schema_definition.py
  - backend/tests/ingestion/test_surreal_production_migration.py
autonomous: true
requirements:
  - SURR-SEARCH-01
  - SURR-SEARCH-02
  - SURR-SEARCH-03
must_haves:
  truths:
    - "SURR-SEARCH-01: Surreal retrieval schema exposes separate indexed title, tags, and text fields for weighted BM25."
    - "SURR-SEARCH-02: Surreal retrieval schema exposes a dimension-aware HNSW index plan for stored embeddings."
    - "SURR-SEARCH-02: Surreal HNSW retrieval is valid only when one active embedding model and one embedding dimension are present in the target."
    - "SURR-SEARCH-03: Surreal relation retrieval keeps TYPE RELATION records and relation metadata available through indexed rel_type and target_id lookup paths."
    - "Phase 42 targets the locally verified SurrealDB 2.0 feature set: SEARCH ANALYZER BM25, HNSW, relation tables, and Python fusion."
  artifacts:
    - path: "backend/src/dotmd/storage/surreal_schema.py"
      provides: "Retrieval index plan, schema fields, and capability report consumed by Surreal-native search engines"
      contains: "build_surreal_native_retrieval_index_plan"
    - path: "backend/src/dotmd/ingestion/migrate_surreal.py"
      provides: "Transform-only lexical field materialization for imported chunks"
      contains: "title"
    - path: "backend/src/dotmd/storage/surreal_schema.py"
      provides: "Scratch-target capability report for required Surreal retrieval features"
      exports: ["SurrealRetrievalCapabilityReport", "probe_surreal_native_retrieval_capabilities"]
    - path: "backend/tests/fixtures/surreal_native.py"
      provides: "Reusable isolated embedded-Surreal fixture for Phase 42 retrieval tests"
      exports: ["isolated_surreal_connection", "apply_surreal_native_retrieval_schema"]
  key_links:
    - from: "backend/src/dotmd/ingestion/migrate_surreal.py"
      to: "backend/src/dotmd/storage/surreal_schema.py"
      via: "chunk payload fields match schema catalog fields"
      pattern: "title.*tags_text"
    - from: "backend/src/dotmd/storage/surreal_schema.py"
      to: "backend/src/dotmd/storage/surreal_schema.py"
      via: "capability probe validates the same analyzer/HNSW/relation statements the schema exposes"
      pattern: "build_surreal_native_retrieval_index_plan"
    - from: "backend/tests/search/test_surreal_native_vector.py"
      to: "backend/tests/fixtures/surreal_native.py"
      via: "Plan 42-02 imports the Plan 42-01-owned fixture without modifying it"
      pattern: "isolated_surreal_connection"
    - from: "backend/tests/search/test_surreal_native_graph.py"
      to: "backend/tests/fixtures/surreal_native.py"
      via: "Plan 42-03 imports the Plan 42-01-owned fixture without modifying it"
      pattern: "apply_surreal_native_retrieval_schema"
---

<objective>
Create the Surreal-native retrieval foundation for Phase 42: schema-level lexical fields, runtime-compatible BM25/HNSW index statements, relation-table consumables, and a scratch-target capability probe.

Purpose: Surreal-native full-text and vector engines cannot be honest if the target schema only preserves body text and raw embeddings. This plan makes the required retrieval fields and index statements explicit before engine work starts.
Output: TDD tests, transform-only lexical field materialization, retrieval index-plan helpers, and capability reporting for the currently verified embedded Surreal runtime.
</objective>

<execution_context>
@/home/j2h4u/.codex/gsd-core/workflows/execute-plan.md
@/home/j2h4u/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-surreal-native-retrieval-implementation/42-RESEARCH.md
@.planning/phases/42-surreal-native-retrieval-implementation/42-PATTERNS.md
@.planning/phases/39-surrealdb-native-retrieval-contract/39-RETRIEVAL-CONTRACT.md
@.planning/phases/41-production-grade-surreal-schema-and-import/41-01-SUMMARY.md
@.planning/phases/41-production-grade-surreal-schema-and-import/41-02-SUMMARY.md
@backend/src/dotmd/storage/surreal_schema.py
@backend/src/dotmd/ingestion/migrate_surreal.py
@backend/src/dotmd/storage/surreal.py
@backend/tests/storage/test_surreal_schema_definition.py
@backend/tests/ingestion/test_surreal_production_migration.py
</context>

<source_audit>
SOURCE | ID | Feature / Requirement | Plan | Status | Notes
GOAL | - | Implement full-text, vector, graph, and hybrid retrieval on real SurrealDB capabilities instead of Phase 38 proxy logic | 42-01, 42-02, 42-03, 42-04 | COVERED | Wave 1 creates index/capability foundation; Waves 2-3 implement engines and fusion.
REQ | SURR-SEARCH-01 | Real SurrealDB BM25/full-text indexes with weighted title, tags, and body/text contributions | 42-01, 42-02 | COVERED | Separate single-field indexes and weighted query composition.
REQ | SURR-SEARCH-02 | SurrealDB vector search using selected HNSW or DISKANN strategy with evidence | 42-01, 42-02 | COVERED | Current runtime path is HNSW per research and local probe; DISKANN requires explicit runtime-upgrade decision outside these plans.
REQ | SURR-SEARCH-03 | Graph/entity retrieval through Surreal relation records preserving relation labels, weights, and metadata | 42-01, 42-03 | COVERED | Relation table consumables and relation-backed graph engine.
REQ | SURR-SEARCH-04 | Hybrid fusion over Surreal result sets with explainable engine attribution | 42-04 | COVERED | Existing Python RRF and SearchCandidate attribution are used over Surreal engine outputs.
RESEARCH | R-42-01 | Use old-style SEARCH ANALYZER BM25 indexes, separate single-field lexical indexes, and weighted score composition | 42-01, 42-02 | COVERED | User constraint and local probe confirm this runtime path.
RESEARCH | R-42-02 | Use HNSW and keep DISKANN upgrade-gated | 42-01, 42-02 | COVERED | No package upgrade or DISKANN syntax is planned.
RESEARCH | R-42-03 | Use TYPE RELATION rows/traversal, not Python full-table relation scans | 42-01, 42-03 | COVERED | Graph engine queries relation records through SurrealQL.
RESEARCH | R-42-04 | Keep fusion in Python because built-in hybrid helpers are rejected locally | 42-04 | COVERED | No built-in hybrid helper dependency.
REVIEW | C1-HIGH-01 | Indexed relation lookup path must support target_id filtering for MENTIONS/HAS_TAG queries | 42-01, 42-03 | COVERED | 42-01 adds/tests `relations_target_id_idx`; 42-03 must query `target_id IN $entity_names` plus allowed `rel_type`.
REVIEW | C1-ACT-01 | HNSW model/dimension contract must be explicit | 42-01, 42-02 | COVERED | Phase 42 uses a single-active-model/dimension-uniformity precondition and fails closed when rows violate it.
REVIEW | C1-ACT-02 | Engine tests must prove real embedded-Surreal retrieval | 42-01, 42-02, 42-03 | COVERED | 42-01 owns `backend/tests/fixtures/surreal_native.py`; 42-02/42-03 import it without modifying it.
REVIEW | C1-ACT-03 | tags_text source metadata key must be pinned | 42-01 | COVERED | `tags_text` derives only from `source_documents.metadata_json["tags"]`, materialized into `documents.metadata["tags"]`.
REVIEW | C1-ACT-04 | hnsw_ef and top_k bounds must be concrete | 42-01, 42-02 | COVERED | Contract is `1 <= top_k <= 100`, `10 <= hnsw_ef <= 400`, and `hnsw_ef >= top_k`, default `hnsw_ef=40`.
REVIEW | C1-ACT-05 | Capability probe consumption must be decided | 42-01 | COVERED | Runtime consumption is explicitly assigned to Phase 43 shadow-run gating or Phase 44 startup wiring; Phase 42 exposes and tests the report only.
CONTEXT | n/a | No Phase 42 CONTEXT.md exists | n/a | EXCLUDED | User prompt says roadmap, research, and prior summaries are authority.
</source_audit>

<review_feedback>
Cycle 1 convergence notes:

| Concern | Severity | Plan change |
|---------|----------|-------------|
| Indexed relation lookup for entity-to-chunk graph retrieval | HIGH | Add `relations_target_id_idx` and a schema test asserting relation lookup DDL includes a single-field `target_id` index alongside `relations_rel_type_idx`; require Plan 42-03 to use `target_id IN $entity_names` plus rel_type filtering. |
| HNSW model/dimension ambiguity | MEDIUM | Phase 42 chooses the single-active-model/dimension-uniformity precondition: the retrieval index plan and vector engine fail closed if target embeddings contain more than one `embedding_model` or if any non-empty vector length differs from `embedding_dimension`. |
| Fake-only engine tests | MEDIUM | This plan creates `backend/tests/fixtures/surreal_native.py` with isolated `surrealkv://` scratch setup and schema application helpers. Plans 42-02 and 42-03 import it and must include mandatory embedded retrieval assertions. |
| `tags_text` metadata key | MEDIUM | `tags_text` derives from `source_documents.metadata_json["tags"]`, the copied frontmatter key already used by filesystem/FTS metadata paths; tests include a known tagged document and assert non-empty output. |
| `hnsw_ef` / top-k bounds | LOW | Shared contract: `1 <= top_k <= 100`, `10 <= hnsw_ef <= 400`, `hnsw_ef >= top_k`, default `hnsw_ef=40`; invalid values raise before SurrealQL construction. |
| Capability probe consumer | LOW | Phase 42 does not wire runtime startup, settings, CLI, MCP, production cutover, or shadow-run execution. The probe report is a typed Phase 42 artifact consumed by Phase 43 shadow-run gating or Phase 44 startup wiring. |
</review_feedback>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED tests for retrieval index fields and capability probing</name>
  <files>backend/tests/fixtures/surreal_native.py, backend/tests/storage/test_surreal_schema_definition.py, backend/tests/ingestion/test_surreal_production_migration.py</files>
  <behavior>
    - Test 1: `build_dotmd_surreal_schema_plan()` exposes `chunks.title` and `chunks.tags_text` as optional string fields while keeping `chunks.text`, `chunks.ref`, and `chunks.document_ref`.
    - Test 2: new `build_surreal_native_retrieval_index_plan(embedding_dimension=3, hnsw_ef=40)` returns analyzer, single-field BM25 index statements for `chunks.title`, `chunks.tags_text`, and `chunks.text`, an HNSW index statement over `embeddings.embedding`, `relations_target_id_idx`, and the existing rel_type/source-target relation indexes.
    - Test 3: the retrieval index plan uses only the runtime-compatible syntax verified in 42-RESEARCH and does not depend on newer full-text syntax, DISKANN, or built-in hybrid helpers.
    - Test 4: the shared validation contract rejects `embedding_dimension <= 0`, multiple active `embedding_model` values, vectors whose length differs from `embedding_dimension`, `top_k` outside 1..100, `hnsw_ef` outside 10..400, or `hnsw_ef < top_k`.
    - Test 5: `load_sqlite_rows_for_surreal()` materializes each chunk payload with `title` from the matching source document and `tags_text` from `source_documents.metadata_json["tags"]` without reading source files, rechunking, embedding, or extracting entities; a known tagged document produces non-empty `tags_text`.
    - Test 6: `backend/tests/fixtures/surreal_native.py` exposes `isolated_surreal_connection(tmp_path)` and `apply_surreal_native_retrieval_schema(connection, embedding_dimension=3, hnsw_ef=40)` for real embedded retrieval tests in Plans 42-02 and 42-03.
    - Test 7: new `probe_surreal_native_retrieval_capabilities()` returns a typed report that marks required BM25, HNSW, relation, and relation-target-index capabilities as passed on a scratch target and records rejected newer-runtime-only features as non-required observations.
  </behavior>
  <action>Write the failing tests first. Extend existing Phase 41 schema and migration tests rather than duplicating fixture setup. Create the shared test helper in `backend/tests/fixtures/surreal_native.py` in this plan only; later engine plans import it but do not edit it, avoiding Wave 2 file conflicts. Pin the `tags_text` key to `source_documents.metadata_json["tags"]` and assert list and scalar tag normalization. Keep tests focused on deterministic statements, payload fields, isolated embedded scratch targets, and typed reports; do not run production targets, package installs, shadow runs, source indexing, or old-stack comparisons.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/storage/test_surreal_schema_definition.py tests/ingestion/test_surreal_production_migration.py -q</automated>
  </verify>
  <done>The targeted tests fail only because the new schema fields, relation target-id index, retrieval index plan, HNSW validation contract, lexical field materialization, embedded fixture helper, and capability module are not implemented yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement retrieval schema fields, index plan, lexical materialization, and capability report</name>
  <files>backend/src/dotmd/storage/surreal_schema.py, backend/src/dotmd/ingestion/migrate_surreal.py, backend/tests/fixtures/surreal_native.py, backend/tests/storage/test_surreal_schema_definition.py, backend/tests/ingestion/test_surreal_production_migration.py</files>
  <behavior>
    - `chunks` rows carry `title` and `tags_text` derived from already captured source document metadata so weighted lexical retrieval has indexed fields.
    - Retrieval index statements are produced through typed schema helpers, not scattered string literals in engine modules.
    - Relation schema planning exposes `relations_target_id_idx` so graph engines can query entity/tag target IDs without depending on the `(source_id, target_id)` composite path.
    - HNSW index planning accepts an explicit embedding dimension, a single active embedding model, and bounded query parameters before generating DDL.
    - Capability probing is safe for scratch targets and does not mutate production URLs, old SQLite stores, TEI, graph exporters, feedback stores, or indexed source files.
  </behavior>
  <action>Update `surreal_schema.py` with typed retrieval index-plan dataclasses, `build_surreal_native_retrieval_index_plan()`, `SurrealRetrievalCapabilityReport`, and `probe_surreal_native_retrieval_capabilities()`. Use the locally verified statement shapes: one analyzer definition, three single-field BM25 indexes on `chunks.title`, `chunks.tags_text`, and `chunks.text`, one HNSW index over `embeddings.embedding`, and a single-field relation index named `relations_target_id_idx` in addition to the existing relation indexes. Implement the shared HNSW contract as constants: `MIN_TOP_K=1`, `MAX_TOP_K=100`, `MIN_HNSW_EF=10`, `MAX_HNSW_EF=400`, `DEFAULT_HNSW_EF=40`, and `hnsw_ef >= top_k`. Add a validation helper that proves the target has one active `embedding_model` and that every non-empty stored vector length equals the caller-supplied `embedding_dimension`; fail closed before DDL/query execution when this precondition is false. Extend `migrate_surreal.py` so chunk payloads include `title` and `tags_text` from `source_documents` rows already loaded from the copied SQLite snapshot; `tags_text` must come from `source_documents.metadata_json["tags"]` only, normalize list or scalar values to a space-separated string, and keep missing metadata as an empty string. Implement `backend/tests/fixtures/surreal_native.py` as the shared isolated embedded fixture for later engine tests. Capability helpers must execute against an isolated `SurrealConnection` and report required feature pass/fail status without upgrading or reinstalling `surrealdb`. Do not wire capability report consumption into runtime startup, settings, CLI, MCP, production cutover, or shadow-run execution; that consumer belongs to Phase 43 shadow-run gating or Phase 44 startup wiring.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/storage/test_surreal_schema_definition.py tests/ingestion/test_surreal_production_migration.py -q</automated>
  </verify>
  <done>Schema tests pass; migration payloads include lexical fields from the pinned `tags` metadata key; relation target-id indexing is present; HNSW validation constants and single-model/dimension checks are tested; capability tests prove required Surreal-native retrieval feature detection; the shared embedded fixture exists; no package install, runtime upgrade, shadow run, cutover, or legacy deletion is introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copied SQLite snapshot -> Surreal chunk payloads | Source metadata is parsed into retrieval fields and must not trigger source-file reads or recomputation. |
| schema/index helpers -> SurrealQL execution | Generated DDL must use fixed identifiers and validated numeric dimensions. |
| scratch capability probe -> local Surreal target | Probe statements mutate only an isolated target chosen by tests or operators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01-01 | Tampering | `build_surreal_native_retrieval_index_plan()` | mitigate | Validate `embedding_dimension` and ef inputs as positive bounded integers; keep table, field, analyzer, and index names fixed constants. |
| T-42-01-02 | Information Disclosure | `load_sqlite_rows_for_surreal()` lexical fields | mitigate | Derive title/tags only from copied snapshot rows already loaded by Phase 41; do not read arbitrary refs or source file paths. |
| T-42-01-03 | Denial of Service | capability probe | mitigate | Require scratch-target usage in tests, keep inserted probe rows tiny, and avoid production target defaults. |
| T-42-01-04 | Tampering | unsupported runtime syntax drift | mitigate | Capability report fails closed when required BM25, HNSW, or relation probes fail; newer-runtime-only features remain observations, not required gates. |
| T-42-01-05 | Denial of Service | relation graph retrieval indexes | mitigate | Add and test `relations_target_id_idx` so entity/tag lookup plans can filter by target_id without a relation-table scan. |
| T-42-01-06 | Tampering | mixed-model HNSW index use | mitigate | Validate a single active embedding model and uniform vector dimension before index/query helpers run. |
| T-42-01-SC | Tampering | package installs | accept | No npm, pip, or cargo install is planned; the existing `surrealdb` dependency is used without upgrade. |
</threat_model>

<verification>
Run the focused gate after each task:

`cd backend && uv run pytest tests/storage/test_surreal_schema_definition.py tests/ingestion/test_surreal_production_migration.py -q`

Before closing the plan, also run:

`cd backend && uv run ruff check src/dotmd/storage/surreal_schema.py src/dotmd/ingestion/migrate_surreal.py tests/fixtures/surreal_native.py tests/storage/test_surreal_schema_definition.py tests/ingestion/test_surreal_production_migration.py`
</verification>

<success_criteria>
Plan 42-01 is complete when the Surreal schema/import surface exposes title, tags, text, embeddings, relation target-id indexing, and relation consumables needed by the engine plans; HNSW single-model/dimension and bounded parameter contracts are explicit; required capability probes are typed and tested; the shared embedded Surreal fixture is available for Wave 2 tests; and all migration changes are transform-only over existing inputs.
</success_criteria>

<output>
Create `.planning/phases/42-surreal-native-retrieval-implementation/42-01-SUMMARY.md` when done.
</output>
