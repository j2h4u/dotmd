---
phase: 43-shadow-run-and-quality-gate
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/dotmd/search/surreal_shadow_metrics.py
  - backend/tests/search/test_surreal_shadow_metrics.py
autonomous: true
requirements:
  - SURR-CUT-01
must_haves:
  truths:
    - "Shadow evidence reports expose quality-adjacent scale and memory metrics without ambiguous missing fields."
    - "Metric validation fails closed when build time, store size, latency, representative corpus, or memory evidence is absent."
    - "Metric JSON preserves production-derived non-ASCII values without forcing recomputation or source reads."
  artifacts:
    - path: "backend/src/dotmd/search/surreal_shadow_metrics.py"
      provides: "Phase 43 metric dataclasses, capture helper, validation, and JSON writer"
      exports: ["ShadowMemoryMetrics", "ShadowMetricBundle", "capture_shadow_memory_metrics", "validate_shadow_metric_bundle", "write_shadow_metric_json"]
    - path: "backend/tests/search/test_surreal_shadow_metrics.py"
      provides: "RED/GREEN coverage for metric shape, fail-closed validation, and JSON serialization"
  key_links:
    - from: "backend/src/dotmd/search/surreal_shadow_metrics.py"
      to: "backend/src/dotmd/search/surreal_parity.py"
      via: "metric bundle embeds evaluate_surreal_scale_gate field names"
      pattern: "evaluate_surreal_scale_gate"
---

<objective>
Create the tested metric contract for Phase 43 shadow-run evidence.

Purpose: SURR-CUT-01 requires latency, index build time, store size, and memory evidence on production-derived data. The runner in later plans needs a small typed surface that preserves Phase 38 scale-gate field names and adds separate memory fields without creating a second quality vocabulary.
Output: A dependency-light `surreal_shadow_metrics.py` module and tests that define exact JSON fields before the runner consumes them.
</objective>

<execution_context>
@/home/j2h4u/.codex/gsd-core/workflows/execute-plan.md
@/home/j2h4u/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-shadow-run-and-quality-gate/43-RESEARCH.md
@.planning/phases/43-shadow-run-and-quality-gate/43-PATTERNS.md
@.planning/phases/40-evaluation-harness-and-golden-queries/40-01-SUMMARY.md
@.planning/phases/42-surreal-native-retrieval-implementation/42-VERIFICATION.md
@backend/src/dotmd/search/surreal_parity.py
@backend/tests/search/test_surreal_retrieval_parity.py
</context>

## Artifacts This Phase Produces

Plan 43-01 creates:

- File: `backend/src/dotmd/search/surreal_shadow_metrics.py`
- File: `backend/tests/search/test_surreal_shadow_metrics.py`
- Dataclass: `ShadowMemoryMetrics`
- Dataclass: `ShadowMetricBundle`
- Function: `capture_shadow_memory_metrics(label, callable_)`
- Function: `validate_shadow_metric_bundle(bundle)`
- Function: `write_shadow_metric_json(path, payload)`

No CLI flag, production runtime setting, backend switch, package install, live index write, or default startup wiring is created by this plan.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for shadow metric completeness</name>
  <read_first>
    @backend/src/dotmd/search/surreal_parity.py
    @backend/tests/search/test_surreal_retrieval_parity.py
    @.planning/phases/43-shadow-run-and-quality-gate/43-PATTERNS.md
  </read_first>
  <files>backend/tests/search/test_surreal_shadow_metrics.py</files>
  <behavior>
    - `ShadowMemoryMetrics` serializes wall-clock seconds, process CPU seconds, max RSS bytes, current Python heap bytes, and peak Python heap bytes as explicit numeric JSON fields.
    - `ShadowMetricBundle` requires the Phase 38 scale fields `passed`, `failure_category`, `recommendation_gate`, `missing`, `record_counts`, `hnsw_build_seconds`, `surrealkv_file_size_bytes`, `query_latency_p50_ms`, and `query_latency_p95_ms`.
    - Validation fails with a `ValueError` naming the missing field when the scale gate or memory payload is incomplete.
    - JSON writing uses UTF-8, sorted keys, trailing newline, and `ensure_ascii=False` so production-derived refs and titles remain readable.
  </behavior>
  <action>Write failing pytest coverage in `backend/tests/search/test_surreal_shadow_metrics.py`. Follow the fixture/helper style from `test_surreal_retrieval_parity.py`. Import the planned identifiers from `dotmd.search.surreal_shadow_metrics` so the first test run fails because the module and symbols do not exist. Cover both passing and missing-field cases, and include one non-ASCII value in a metadata/sample field to prove the JSON writer preserves it. Do not require Docker, TEI, FalkorDB, a live Surreal target, or production files.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/search/test_surreal_shadow_metrics.py -q</automated>
  </verify>
  <acceptance_criteria>Focused RED tests exist and fail only because the planned metric module is missing or incomplete.</acceptance_criteria>
  <done>RED metric tests define the Phase 43 metric contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN metric helpers and JSON writer</name>
  <read_first>
    @backend/src/dotmd/search/surreal_parity.py
    @backend/tests/search/test_surreal_shadow_metrics.py
    @.planning/phases/43-shadow-run-and-quality-gate/43-RESEARCH.md
  </read_first>
  <files>backend/src/dotmd/search/surreal_shadow_metrics.py, backend/tests/search/test_surreal_shadow_metrics.py</files>
  <behavior>
    - `capture_shadow_memory_metrics(label, callable_)` returns the callable result plus `ShadowMemoryMetrics`, using `time.perf_counter`, `time.process_time`, `resource.getrusage`, and `tracemalloc`.
    - `validate_shadow_metric_bundle(bundle)` accepts a complete scale gate from `evaluate_surreal_scale_gate()` plus memory metrics and rejects null or absent required fields.
    - `write_shadow_metric_json(path, payload)` creates parent directories and writes deterministic UTF-8 JSON.
  </behavior>
  <action>Implement `backend/src/dotmd/search/surreal_shadow_metrics.py` with `from __future__ import annotations`, stdlib-only imports, frozen/slotted dataclasses, typed helper functions, and no import of SurrealDB, TEI, `DotMDService`, Docker clients, or storage backends. Preserve Phase 38 scale-gate field names exactly; add memory evidence as a separate nested object rather than merging it into the scale gate. Keep validation fail-closed and deterministic. Export the planned identifiers through `__all__`. Do not run `dotmd index`, load production indexes, call TEI, call GLiNER, query feedback.db directly, switch runtime defaults, or add package dependencies.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/search/test_surreal_shadow_metrics.py tests/search/test_surreal_retrieval_parity.py -q</automated>
    <automated>cd backend && uv run ruff check src/dotmd/search/surreal_shadow_metrics.py tests/search/test_surreal_shadow_metrics.py</automated>
  </verify>
  <acceptance_criteria>The focused metric suite passes, existing scale-gate tests still pass, and the new module has no live-service side effects.</acceptance_criteria>
  <done>Metric helpers pass RED/GREEN tests and are ready for the shadow runner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production-derived evidence -> metric JSON | Source refs, counts, and sample labels cross into repo-local artifacts. |
| runner process -> OS resource APIs | Wall-clock, CPU, RSS, and heap measurements are observed from the local process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information Disclosure | `write_shadow_metric_json` | mitigate | Write only explicitly supplied metrics and samples; preserve redaction metadata from later runner artifacts; do not read source files or feedback.db here. |
| T-43-02 | Tampering | metric payload validation | mitigate | `validate_shadow_metric_bundle()` rejects absent scale or memory fields instead of producing a passing report with partial evidence. |
| T-43-03 | Repudiation | metric report provenance | mitigate | Include labels and complete field names so later runner artifacts can tie metrics to one source-capture manifest and command invocation. |
| T-43-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs are planned; stdlib-only implementation avoids package legitimacy risk. |
</threat_model>

<verification>
Plan 43-01 is complete when `cd backend && uv run pytest tests/search/test_surreal_shadow_metrics.py tests/search/test_surreal_retrieval_parity.py -q` passes and `ruff check` passes on the new metric files.
</verification>

<success_criteria>
- The metric module exists and exports the planned dataclasses/functions.
- Missing build time, store size, latency, representative corpus, or memory fields cannot pass validation.
- JSON output is deterministic, UTF-8, and readable for non-ASCII production-derived evidence.
- No production cutover, runtime default switch, fallback backend, legacy deletion, rechunking, reembedding, or entity re-extraction is introduced.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | phase goal | Compare old stack and SurrealDB stack on production-derived data and classify every material difference. | 43-01, 43-02, 43-03 | COVERED | This plan supplies the metric contract used by the later comparison runner. |
| REQ | SURR-CUT-01 | Record quality, latency, build-time, store-size, and memory evidence. | 43-01, 43-02, 43-03 | COVERED | This plan covers latency/build/store/memory field validation. |
| REQ | SURR-EVAL-03 | Regressions block cutover unless fixed or explicitly accepted. | 43-02, 43-03 | COVERED | Covered by the runner and final acceptance ledger plans. |
| RESEARCH | bounded evidence window | One source-capture manifest, baseline capture, Surreal target, metrics bundle, and acceptance ledger. | 43-01, 43-02, 43-03 | COVERED | Metric bundle is the first contract in that sequence. |
| RESEARCH | reuse Phase 38 scale fields | Preserve `evaluate_surreal_scale_gate()` field names. | 43-01 | COVERED | Required in task behavior and key links. |
| CONTEXT | user prompt | Shadow/evidence only; no production cutover, runtime default switch, legacy deletion, fallback, or compatibility shims. | 43-01, 43-02, 43-03 | COVERED | Every plan states the scope boundary and verification checks it. |
| CONTEXT | user prompt | Preserve migrated data and avoid rechunking, reembedding, or entity re-extraction unless no safe transform path exists. | 43-02, 43-03 | COVERED | The runner and final execution use Phase 41 transform-first artifacts. |
| CONTEXT | user prompt | Candidate retrieval uses Phase 42 explicit overrides, not startup wiring. | 43-02, 43-03 | COVERED | Runner plan owns explicit override consumption. |
</source_coverage_audit>

<output>
Create `.planning/phases/43-shadow-run-and-quality-gate/43-01-SUMMARY.md` when done.
</output>
