---
context: default
phase: null
status: in_progress
last_updated: 2026-04-26T16:53:46Z
---

# No blocking constraints.

<current_state>
Ad-hoc session: MCP improvements + E2E tests. HTTP E2E 12/12 pass. Stdio E2E 0/12 — 2 bugs diagnosed.
Last commit: ed80d3b. HANDOFF.json has full state.
</current_state>

<completed_work>
- 999.3 + 999.10 → DONE (backlog)
- search() surface cleanup (removed mode/rerank/matched_engines/start_time)
- drill(file_path) MCP tool added
- MCP tools/call fixed (3 root causes: async, stateless_http, live_diff)
- E2E tests/e2e/ created — 12 tests, HTTP 12/12 pass
- E2E parametrized over http+stdio — stdio WIP (commit ed80d3b)
- 999.12 + 999.13 added to backlog
</completed_work>

<remaining_work>
Bug 1: dotmd mcp (stdio) bypasses create_app() → _service=None
  Fix: lazy init in _get_service() in mcp_server.py

Bug 2: anyio cancel scope cross-task in _StdioSession.stop()
  Fix: background thread + asyncio.Queue + run_coroutine_threadsafe
  Pseudocode in full handoff — see HANDOFF.json or re-ask Claude

After: pytest tests/e2e/ → 24/24 (12×2 transports)
</remaining_work>

<decisions_made>
- drill not get_document (semantic)
- stateless_http=True + json_response=True (mcp 1.27.0 SSE bug workaround)
- live_diff=False for MCP status() (was 15s scan)
- E2E inside container vs http://localhost:8080
</decisions_made>

## Required Reading
1. backend/tests/e2e/conftest.py — current broken _StdioSession
2. backend/src/dotmd/mcp_server.py — _get_service(), tool definitions
3. backend/src/dotmd/cli.py line 401 — stdio bypass path

## Infrastructure State
- dotmd: running, stateless+json_response, port 8080 internal
- pytest not in image: pip install pytest needed on restart

<next_action>
Fix _get_service() lazy init → fix _StdioSession thread+queue → pytest 24/24 green
</next_action>
