---
context: default
phase: null
task: null
status: paused
last_updated: 2026-04-25T14:36:14.000Z
---

# Ad-hoc session — паузa

Нет активной фазы. Сессия была набором точечных улучшений.

<current_state>
Контейнер `dotmd` запущен и healthy. Два процесса внутри (start.sh): serve (8000) + mcp HTTP (8080).
Всё работает, но архитектура ещё не оптимальна — два DotMDService в памяти.
Следующий шаг: GSD Quick для объединения в один процесс.
</current_state>

<completed_work>

Все изменения не закоммичены — сделать `git commit` в начале следующей сессии.

Изменённые файлы: `.mcp.json`, `AGENTS.md`, `CLAUDE.md`, `backend/Dockerfile`, `backend/start.sh`,
`backend/src/dotmd/api/server.py`, `backend/src/dotmd/cli.py`, `backend/src/dotmd/mcp_server.py`, `docker-compose.yml`

Что сделано:
- Убрали дублирующиеся health check логи: `access_log=False` + `/health` → DEBUG уровень
- CLAUDE.md переписан как `@AGENTS.md`, AGENTS.md расширен актуальным стеком
- Очищены orphan `vec_chunks_*` строки (1519 строк из 3 таблиц) — trickle падал на 2 файлах из-за них
- Удалены qwen3 embedding таблицы из production DB (эксперимент заморожен) + VACUUM
- Добавлен streamable-http транспорт в MCP сервер (порт 8080)
- Слито в один контейнер `dotmd` через `start.sh` (было `dotmd-api-1` + `dotmd-mcp-1`)
- Обновлён `.mcp.json`: `dotmd-api-1` → `dotmd`
- MCP best practices: аннотации (`readOnlyHint`, `idempotentHint`, `title`), `Literal["hybrid","semantic","keyword","graph"]`, `Annotated` + `Field` на всех параметрах, расширенные `instructions`
</completed_work>

<remaining_work>

**GSD Quick: унификация архитектуры**

Цель: один процесс, один DotMDService, ~200MB памяти освобождается.

Шаги:
1. Проверить как FastMCP добавляет custom routes (для `/health` на порту 8080)
2. Добавить `/health` в `mcp_server.py`
3. Перенести trickle lifespan из `api/server.py` в `mcp_server.py` (FastMCP поддерживает `lifespan=` параметр)
4. Обновить `start.sh` — убрать `serve`, оставить только `mcp`
5. Обновить `Dockerfile` HEALTHCHECK: порт `8000` → `8080`
6. Пересобрать образ (`docker compose build dotmd`)
7. Перезапустить контейнер
8. Проверить: `docker ps` (healthy), логи (trickle стартовал), MCP поиск работает
</remaining_work>

<decisions_made>

- **Один процесс**: REST serve убирается целиком. MCP HTTP — единственный долгоживущий процесс.
- **CLI оставить**: не используется в prod, но убирать не надо — без затрат.
- **Healthcheck**: переедет на порт 8080 (FastMCP uvicorn), custom `/health` route.
- **stdio MCP** (`.mcp.json`): остаётся `docker exec -i dotmd dotmd mcp` — не меняется.
</decisions_made>

<blockers>
Нет.
</blockers>

## Infrastructure State

- Контейнер `dotmd`: running, healthy, bind-mount из репо
- `dotmd_dotmd-index` volume: migrated, schema v16 M2M
- TEI (`embeddings`): запущен
- FalkorDB (`graphiti-falkordb-1`): запущен
- Trickle: в watch mode (все 15 файлов обработаны, 2 упали — но orphan cleanup сделан, на следующем hourly scan должны проиндексироваться)

<context>
Сессия была разбором технического долга и улучшением инфраструктуры. Главное открытие: REST serve — legacy от upstream, никем не используется. Вся ценность — trickle внутри него. Следующий шаг логичен и хорошо определён.
</context>

<next_action>
1. Сначала `git commit` всех изменений этой сессии
2. Затем `/gsd-quick` — унификация архитектуры (описание в HANDOFF.json → next_action)
</next_action>
