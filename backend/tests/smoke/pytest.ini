[pytest]
# Prevent pytest from loading parent conftest.py (which imports dotmd).
# Smoke tests are external HTTP tests — they must not depend on dotmd.
markers =
    smoke: smoke tests requiring a running dotMD stack
